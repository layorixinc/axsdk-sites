# RPC Lua 런타임 요청 18 — 결정적 노드가 컨텍스트를 읽을 방법, 그리고 커뮤니티 스크립트 op

날짜: 2026-08-22 · 대상: agentv3 runtime · 작성: axsdk-sites
근거: 라이브 실측. `check:flows` 187 · `test:lua` 575 · extension 1028 · 답변 경로 라이브 통과.
관련: 요청 15/16(런타임 도구는 `parameters.properties`로 투영된다 — 그 규칙은 이번에도 정확했습니다)

**요청 2건.** 둘 다 없어도 진행할 우회로가 있고, 그 우회로도 아래에 적었습니다. 결함 보고가 아니라
**능력 요청**입니다.

---

## 1. 배경 — 무엇을 만들고 있나

Chrome `userScripts` API로 **커뮤니티 스크립트**를 실행하는 기능입니다. 사용자가 서명된 릴리스를
설치하면 확장이 그것을 페이지에 등록하고, 사용자가 자연어로 물으면 그 스크립트가 읽은 값으로
답합니다. 이미 라이브로 동작합니다.

```text
$ send '이 페이지 뭐라고 쓰여 있어?'
페이지의 제목은 "AXSDK community fixture page"입니다. (읽은 스크립트: fixture.read-page/read_heading)
```

동작하는 부분은 **인자 없는 read**입니다. 확장이 턴 전에 미리 실행해 결과를 컨텍스트로 넣습니다.

동작하지 않는 부분은 **인자를 받는 명령**입니다. 사용자가 "이 스크립트의 X를 실행해줘"라고 하면
확인 버튼을 렌더링해야 하는데, 그 결정을 내리는 노드가 카탈로그를 읽지 못합니다.

---

## 2. 결론

| 요청 | 내용 | 없으면 |
|---|---|---|
| **R18-1** | `action_contract`가 선언된 컨텍스트를 읽을 방법 | 플래너에게 인텐트를 하나 더 맡깁니다(아래 §3.4) |
| **R18-2** | 클라이언트 op 어휘에 `community.catalog` / `community.invoke` | 위젯 버튼 + 확장 AX 핸들러로 우회합니다(§4.3) |

---

## 3. R18-1 — 결정적 노드가 컨텍스트를 읽을 방법이 없습니다

### 3.1 관측

`action_contract` 노드가 `contexts.community`를 `inputSelector`에 넣고, 도구가
`parameters.properties`에 같은 이름을 선언했습니다. 스크립트 안에서 받은 인자를 그대로 출력했습니다.

```text
catalog=0  text=51  keys=requestText
```

`requestText`는 건너옵니다. **`contexts.community`는 인자 객체에 아예 없습니다** — 빈 문자열이
아니라 부재입니다.

같은 컨텍스트를 **터미널의 `respond`가 선택하면 정상적으로 읽힙니다.** 라이브로 확인했습니다 —
위의 답변이 그 경로입니다. 즉 컨텍스트는 **프롬프트에는 도달하고 도구 인자에는 도달하지 않습니다.**

### 3.2 이것이 규칙인지 결함인지

요청 16에서 확인해 주신 규칙 — *"런타임 도구는 `parameters.properties`로 투영된다"* — 과 모순되지
않습니다. 컨텍스트는 flow state가 아니니, 점 찍힌 이름을 선언해도 채울 것이 없는 속성이 하나
생기는 것뿐입니다. **그래서 결함 보고가 아닙니다.**

다만 저희 쪽 `check:flows`는 "노드가 선택한 이름을 도구가 선언했는가"를 검사해서 **통과시킵니다.**
게이트는 초록이고 런타임은 조용히 버립니다. 이 조합이 라이브 실행 세 번을 쓰게 했습니다.

### 3.3 요청

셋 중 하나면 충분합니다. 선호는 위에서부터입니다.

1. **선언된 컨텍스트를 런타임 도구 인자로 투영해 주세요.** `inputSelector`에 `contexts.<name>`이
   있고 도구가 같은 이름을 선언했을 때만. 새 문법이 없습니다.
2. 또는 **컨텍스트를 flow state로 승격하는 방법**을 알려 주세요 (예: 노드에서
   `stateFromContext: [community]` 같은 선언). 저희가 모르는 기존 경로가 있다면 그것으로 충분합니다 —
   요청 17이 정확히 그런 경우였습니다.
3. 또는 **불가하다고 확정해 주세요.** 그러면 §3.4로 갑니다. 확정만 있으면 됩니다.

### 3.4 없으면 이렇게 합니다

라우터 인텐트를 하나 더 만듭니다. `community_script`(묻기)와 `community_run`(실행 요청)을 나누고,
`community_run`의 진입 노드에는 **`answer` 분기를 두지 않습니다.** 플래너가 이미 여덟 개 인텐트에
대해 하는 일을 하나 더 하는 것이고, 라우팅된 노드는 산문으로 답할 길이 없습니다.

이 우회로는 동작할 것으로 보지만, 결정을 **모델에게 남깁니다.** R18-1이 되면 그 결정이
결정적(deterministic)이 되고, 저희가 이미 작성해 둔 분류기(18개 단위 테스트)가 그대로 붙습니다.

### 3.5 왜 프롬프트로 안 되는지 (측정)

"모델에게 제안하라고 시키면 되지 않나"는 **네 번 시도해서 네 번 실패했습니다.** 원래 프롬프트,
날카롭게 다듬은 프롬프트, 도구 분리, 카탈로그 라벨 변경. 매번 산문으로 답했습니다. 원인은 그 노드에
`answer` 분기가 **항상 열려 있다**는 것이었습니다. `AGENTS.md`의 저희 규칙대로 세 번째 형태에서
멈추고 구조를 바꾸기로 했습니다.

---

## 4. R18-2 — `community.*` 클라이언트 op

### 4.1 관측

`tools/rpc-allow.mjs`의 op 목록은 **`GET /axsdk/v2/lua/ops`를 미러링**한 것입니다. 그래서 확장이
클라이언트 쪽에 op을 구현해도, 플로우가 `rpc.allow`에 적을 수 없습니다.

호스트 op은 실재합니다 — 세션 워커가 `tabs.list/create/close/activate`를 이름으로 전달합니다.
그런데 `_common/flows.yaml`과 `_common/rpc` 전체에서 `tabs.*`를 grant하는 플로우가 **하나도
없습니다.** 선례가 없어 저희가 가정할 수 없습니다.

### 4.2 요청

**질문 하나가 먼저입니다.** 플로우가 서버가 공표하지 않은 호스트 op을 `rpc.allow`에 적을 수
있습니까?

- **가능하다면** R18-2는 취소입니다. 저희가 클라이언트에 구현하고 끝냅니다.
- **불가하다면** 아래 두 op을 어휘에 추가해 주시기를 요청합니다.

```text
community.catalog()                                   -> 설치·활성 상태의 명령 목록(메타데이터만)
community.invoke({ scriptId, version, command, args }) -> 결과 또는 분류된 거부
```

두 op 모두 **확장이 이미 구현했고 라이브로 동작합니다.** 브로커가 설치·활성·미폐기·버전·선언된
명령·인자 스키마·승인된 효과·동의를 전부 재도출합니다. 필요한 것은 플로우가 그것을 부를 수 있는
어휘뿐입니다.

`community.invoke`는 **mutation일 수 있으므로**, 다른 mutation op과 같은 취급이면 됩니다. 확장이
호출마다 사용자 확인을 받고, 결과가 불확실하면 재시도하지 않습니다.

### 4.3 없으면 이렇게 합니다

위젯 버튼 → `ax` 액션 → 확장 AX 핸들러 → 브로커. 이미 만들었고 테스트·뮤테이션 검증까지
끝냈습니다(`AX_widget_community_invoke`, 정확한 허용목록 1개). 사용자가 버튼을 눌러야 하므로
**턴이 하나 더 듭니다.** R18-2가 되면 그 왕복이 사라집니다.

---

## 5. 현재 상태 (참고)

| | |
|---|---|
| 라이브 동작 | 서명 릴리스 설치·검증·캐시 → `chrome.userScripts` 등록 → 핸드셰이크 → 인자 없는 read 사전 실행 → **단일 턴 자연어 답변** |
| 브로커 | 명령 디스패치, 인자 스키마 검증, 효과별 동의, mutation 재시도 없음, `storage.*`, 선언 호스트 `net.fetch` |
| 게이트 | `check:flows` 187 · `test:lua` 575 · extension 1028 · 라이브 스모크 3종 |
| 막힌 것 | 인자를 받는 명령의 제안 경로 하나 |

**두 요청 모두 없어도 §3.4 + §4.3으로 진행합니다.** 회신 주시면 그에 맞춰 방향을 정리하겠습니다.
