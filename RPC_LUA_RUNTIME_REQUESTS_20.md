# RPC Lua 런타임 요청 20 — 일시정지된 플로우가 짧은 응답을 가져가야 합니다

날짜: 2026-08-26 · 대상: agentv3 runtime (플래너/라우터) · 작성: axsdk-sites
근거: 라이브 실측 24턴 + 완화 후 16턴. `check:flows` 191 · `test:lua` 593 · `test:scenarios` 104 ·
`test:playground` 87 · `check:bundle` ok(32 assets).

**요청 1건.** 사용자가 질문을 받고 답했는데 그 답이 **다른 플로우로 라우팅되고, 답을 기다리던 플로우가
파기**됩니다. 프롬프트 쪽 완화는 이미 적용해 비율을 10/24 → 1/16으로 낮췄지만, 완화는 통계이고
파기는 결정적입니다. 엔진 규칙 하나를 요청드립니다.

---

## 1. 요청 — 질문을 들고 있는 플로우가 짧은 응답의 기본 수신자여야 합니다

### 1.1 지금 무슨 일이 일어나는가

`shopping_single_site`가 `refine_item`에서 목록을 제시하고 멈춥니다(질문: 어떤 기준으로 고를까요).
사용자가 **"첫 번째로 해줘"** 라고 답합니다. 플래너가 이 턴을 `replace_current`로 판단하고
`shopping_multi_store_total_cost`(또는 `shopping_search_plan`)를 시작합니다.

측정된 결과는 두 가지이고, 두 번째가 본 요청의 이유입니다.

1. 사용자는 1~2분짜리 새 비교 안내("비교하려는 제품과 비교할 사이트를 알려 주세요")를 받습니다.
2. **일시정지 노드가 남아 있지 않습니다.** 같은 세션에서 곧바로 같은 문장을 다시 보내면
   `shopping_single_site.collect_shopping`으로 **새로 진입**해 "어떤 상품을 구매하고 싶으신가요?"를
   묻습니다. 실제 검색(내비게이션 + 30행)과 사용자의 선택지가 사라집니다.

즉 **재시도로 복구되지 않습니다.** 이 때문에 도착한 플로우의 엔트리 노드에 결정적 가드를 두는 방법이
쓸 수 없습니다 — 그 노드가 실행되는 시점에는 이미 교체가 끝나 있습니다.

### 1.2 실측

| 조건 | 미스라우팅 / 픽 턴 |
|---|---|
| 기준선 (플래너 예시가 실제 요청과 같은 제품을 명명) | **10 / 24** |
| 완화 후 (예시 제품만 교체) | **1 / 16** |

- 대상 쌍: eBay + `"이 사이트에서 Logitech M185 mouse 사줘"` → `"첫 번째로 해줘"`.
- 도착지: `shopping_multi_store_total_cost.collect_request` 다수, `shopping_search_plan` 2회,
  **노드 자체에 도달하지 않은 턴** 1회.
- 한 세션 안에서 측정했습니다(두 CLI 프로세스는 두 세션이라 다중 턴 주장을 할 수 없음).
- 편차가 큽니다: 2/4 직후에 5/5 무결 런이 나왔습니다. **단일 무결 런은 증거가 되지 않습니다** —
  각 조건 8턴 이상을 권합니다.

### 1.3 우리 쪽에서 이미 한 것

- 플래너 프롬프트에 `SINGLE-SITE SHOPPING FOLLOW-UP` 규칙이 이미 있습니다:
  `activeFlow=shopping_single_site` 이고 `activeNode`가 `collect_shopping`/`refine_item`/
  `checkout_confirm`이면 `continue_current`로 해당 플로우를 잇고 메시지를 **그대로** `state.requestText`에
  복사하라는 지시입니다. 즉 조건은 이미 기계 판정 가능한 형태로 적혀 있고, 모델이 그것을 어깁니다.
- 원인 하나를 찾아 제거했습니다: 플래너 예시 두 곳(`Examples:` 한 줄과 라우트 `examples` 한 줄)이
  **사용자가 방금 말한 것과 같은 제품**을 비교 인텐트의 예시로 명명하고 있었고, 그중 하나는 `이베이`까지
  함께 명명했습니다. 제품명을 교체해 위 표의 1/16을 얻었습니다. 양성 대조로 실제 비교 요청은 여전히
  비교 플로우로 들어갑니다(2/2, 그중 하나는 `Logitech M185`를 문자 그대로 포함).
- 프롬프트 재작성은 여기서 멈춥니다. 저희 기준으로 문구 시도는 3회에서 종료하며, 이 건은 문구 문제가
  아니라 **예시와 대화의 어휘 충돌**이었고 그 충돌은 어떤 예시 제품으로도 다시 생길 수 있습니다.

### 1.4 요청하는 규칙

플래너의 자유도를 좁히는 결정적 규칙 하나입니다.

> 어떤 플로우가 **사용자에게 질문을 제시한 상태로 일시정지**되어 있고, 새 메시지가 **새 요청을 명시적으로
> 지시하지 않는 짧은 응답**이면, 그 턴은 일시정지된 플로우로 간다.

- 판정 근거는 상태입니다: 일시정지 노드가 존재하고 그 노드가 질문을 냈다는 사실은 엔진이 알고 있습니다.
- "명시적 새 요청"의 예: 다른 인텐트의 동작 동사 + 새 목적어("청소 견적 줘", "이거 결제해줘"),
  또는 사용자가 취소를 말하는 경우(취소는 이미 각 게이트가 처리합니다).
- 애매하면 **일시정지 플로우 우선**이 안전한 기본값입니다. 잘못 이어붙이면 그 플로우가 한 번 더 묻고
  끝나지만, 잘못 교체하면 위 1.1처럼 사용자의 작업이 사라집니다. 두 오류의 비용이 대칭이 아닙니다.

### 1.5 대안 (엔진 변경이 어렵다면)

플래너 결정을 그대로 두더라도, **교체될 플로우의 일시정지 노드를 파기하지 말고 보존**해 주시면
사용자가 같은 문장을 한 번 더 말해 복구할 수 있습니다. 지금은 재시도가 새 진입이 되어 복구 경로가
없습니다. 1.4가 어렵다면 이것만이라도 요청드립니다.

## 2. 참고 — 우리 쪽 기록

`AGENTS.md` §13에 원인·수치·완화·한계를 함께 적었습니다("예시는 장식이 아니라 플래너가 매 턴 보는
학습 데이터다 — 자기 라이브 시나리오가 입력하는 제품으로 인텐트를 예시하지 말 것").
