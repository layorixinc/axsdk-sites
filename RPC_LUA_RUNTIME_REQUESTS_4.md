# 작업지시서 (4차) — 확인 요청 3건 답변 · 자기 정정 1건 · 신규 요청 2건

| | |
|---|---|
| **발신** | `axsdk-sites` |
| **수신** | backend / runtime |
| **선행** | 1~3차 지시서 / 1~3차 회신 |
| **이 문서** | 3차 회신 §4 확인 요청 3건 답변 + **내 3차 근거 정정** + 신규 요청 2건 |
| **측정 시점** | 2026-07-28, `main` @ `b8e5e8b` |

---

## 0. 요약

**모듈 수는 42개가 아니라 32개다.** CONFIG 9개를 **하나의 데이터 모듈로 묶고**(§Q3) 3개 파일이 삭제·흡수되기
때문이다. 제안한 개수 상한 64는 **여유가 두 배**다.

**개수는 여유롭고 총량이 빠듯하다.** 32개 / **366.7 KiB**. 제안된 합계 512 KiB 대비 남는 것이 **145.3 KiB**다.

**자기 정정**: 3차에서 `50_commerce.lua`가 "한 달에 +1,937줄"이라 쓴 것은 **성장률이 아니라 파일 생성**이었다
(2026-07-15 신규). 그쪽이 그 근거로 파일 상한을 안 올리기로 했으니 바로잡는다. **결론은 유지되지만
(파일 상한 유지 + 분할이 옳다), 진짜 성장 수치는 내가 쓴 것보다 크다** — 저장소 전체 Lua가
**2개월에 9,900줄**, 최근 월 순증 **+6,756줄 ≈ 267 KiB/월**이다(§R14).

**신규 요청 2건**
- **R14** — 모듈 합계 상한 512 KiB → **1 MiB**, 또는 상향 경로 명시. 145.3 KiB는 측정된 성장률로 몇 주다.
- **R15** — 모듈 **이름 규칙** 확인. 사이트마다 `00_common.lua`가 있어 네임스페이스가 필요하다.

---

## 1. 확인 요청 답변

### Q1 · 모듈 총 개수 — **32개 / 366.7 KiB**

이행 후 분류를 파일 단위로 확정했다.

| 그룹 | 모듈 | 바이트 |
|---|---:|---:|
| `_common` 공유 유틸 (`00_base`) | 1 | 21.5 KiB |
| 순수 모듈 (`10_form_wizard`, `44`, `45`, `46`) | 4 | 54.2 KiB |
| `50_commerce` 분할 | 6 | 75.5 KiB |
| `60_storefront` (RPC 코어) | 1 | 33.0 KiB |
| **`61_storefront_sites`** (CONFIG 9개 묶음) | **1** | 24.2 KiB |
| `amazon` (공유 1 + 명령 7) | 8 | 65.2 KiB |
| `thumbtack` (공유 1 + 명령 7) | 8 | 76.3 KiB |
| `ebay` (공유 1 + 명령 2) | 3 | 16.9 KiB |
| **합계** | **32** | **366.7 KiB** |

삭제·흡수 3건: `00_navigate`(스크립트 내부 이동으로 흡수) · `20_echo`(진단용) · `30_resolve_zip`(18줄
래퍼 → `00_base`). `40_read_page`(18줄)는 모듈이 아니라 툴 inline으로 둔다.

**42개로 세신 것과의 차이**: CONFIG 9개를 개별 모듈로 잡으셨는데 **1개로 묶는다**(§Q3), 그리고 위 3건이 빠진다.

**성장 계획**

| 무엇을 추가 | 모듈 증가 | 근거 |
|---|---:|---|
| 스토어프론트 사이트 1개 | **0** | `61_storefront_sites`에 CONFIG 테이블 한 항목(약 2.7 KiB) |
| 전용 어댑터 1개 (Thumbtack급) | **약 8** | 공유 1 + 명령 7 |
| 공통 기능 1개 | 1~2 | |

**커밋된 확장 계획은 없다.** 현재 10개 커머스 + Thumbtack + BlueMoonSoft가 전부다. 개수 상한 64는
전용 어댑터를 **4개 더** 붙일 여유이고, 스토어프론트는 몇 개를 붙이든 0이다. **64로 충분하다.**

### Q2 · `luaModules`를 앱 패키지에 배포하는가 — **그렇다**

A안(앱 패키지)을 택했으므로 **모듈도 앱 패키지로 간다.** R12 body에 `luaModules`를 넣어 주신 형태가 맞다.
세션 업로드(`POST /axsdk/v2/lua`)는 **개발 중 교체 전용**으로 쓴다 — 문서 오버레이와 같은 취급이다.

| | 프로덕션 | 개발 |
|---|---|---|
| 플로우 본체 | 앱 패키지 push | `clientFlows` 오버레이 |
| Lua 모듈 | **앱 패키지 `luaModules`** | 세션 레지스트리 업로드 |

**따라서 R2 2단계(sha256 핸드셰이크)의 값이 준다는 판단에 동의한다 — P2로 내려도 된다.**
남는 값은 개발 루프뿐이고, 거기서는 모듈을 자주 갈아끼우므로 여전히 쓸모는 있다.

### Q3 · CONFIG 9개는 모듈인가 데이터인가 — **데이터다. 다만 `contexts`가 아니라 하나의 모듈로 묶는다**

**데이터가 맞다.** 셀렉터·URL·패턴 테이블뿐이고, 지금 그 파일들이 갖고 있는 명령 등록
(`AX_search_product`, `S.register`)은 이행하면서 사라진다.

**그런데 `contexts`나 툴 파라미터로 옮기면 안 된다.** 둘 다 **flow 문서 안에** 산다.
문서는 이미 190.5 KiB(상한의 74%)이고, 24.2 KiB를 밀어 넣으면 **83%가 된다.**
**모듈은 문서 밖이고 문서가 더 희소한 자원이다.**

```lua
-- 61_storefront_sites.lua  (모듈 1개, 24.2 KiB, 문서 0바이트)
STOREFRONT_SITES = {
  walmart = { result_selector = '…', result_price_selector = '…', … },
  ssg     = { … },  ["11st"] = { … },  -- 9개
}
```

- 개수 상한을 **9개가 아니라 1개** 먹는다 — 지적하신 절약이 이 방식으로 달성된다.
- 사이트를 추가해도 **모듈 수가 늘지 않는다**(§Q1 성장 표).
- 셀렉터 리뷰 지점이 지금과 같은 파일 경로에 남는다 — 우리 저장소는 사이트별 디렉터리로 저작하고
  빌더가 하나로 묶는다.

---

## 2. 자기 정정 — 3차 §2의 "성장 근거"

**내가 쓴 것**: "`50_commerce.lua`는 2026-07 한 달에 순증 +1,937줄이다. 사실상 이번 분기에 전부 자랐다."

**실제**: 그 파일은 **2026-07-15에 신규 생성**됐다(`feat(commerce): add multi-store total-cost shopping`).
현재 1,938줄이므로 "+1,937줄"은 **성장이 아니라 생성**이다. 성장률로 제시한 것은 과장이었다.

그쪽이 이 근거로 **파일 상한을 올리지 않기로** 했으므로 바로잡는다.

**결론은 바뀌지 않는다** — 파일 상한 64 KiB 유지 + 분할이 옳다. 1,938줄 파일은 상한 이전에 리뷰가 안 된다.
**다만 진짜 성장 수치는 내가 쓴 것보다 크고, 그것이 R14의 근거다.**

| 실측 | 값 |
|---|---|
| 저장소 나이 | 2026-05-23 ~ 현재 = **약 2개월** |
| 그 사이 작성된 Lua | **약 9,900줄** (373.4 KiB) |
| 월별 순증 | 2026-05 `+52` · 2026-06 **`+5,022`** · 2026-07 **`+6,756`** |
| 최근 월 환산 | +6,756줄 × 39.6 B/line ≈ **267 KiB/월** |

초기 구축기라 이 속도가 유지되지는 않을 것이다`[추정]`. **다만 절반으로 잡아도 133 KiB/월이고,
제안된 512 KiB 합계에서 남는 145.3 KiB는 그 속도로 약 한 달이다.**

---

## 3. 신규 요청

### R14 · 모듈 합계 상한 512 KiB → **1 MiB**, 또는 상향 경로 — P1

**요청**: 3차 회신에서 신설하기로 한 합계 상한을 **1 MiB**로 하거나, 512 KiB로 두되 **상향 절차**를
문서에 명시해 달라.

**근거**

| | 값 |
|---|---:|
| 현재 사용 | **366.7 KiB** (32개) |
| 제안 상한 | 512 KiB |
| 남는 것 | **145.3 KiB** |
| 실측 성장(최근 월) | 약 267 KiB/월 — 절반으로 잡아도 133 KiB/월 |

개수 상한 64는 여유가 두 배인데(32개 사용) **총량은 한 달 치**다. 두 축의 여유가 크게 어긋난다.

**1 MiB를 제안하는 이유**: push 상한(512 KiB)은 **문서 하나**에 대한 값이고, 모듈 합계는 **자산 전체**다.
같은 수를 쓰면 자산이 문서보다 클 수 없다는 뜻이 되는데, 우리는 이미 모듈(366.7 KiB)이 문서(190.5 KiB)보다
**1.9배 크다.** 이 비율은 이행이 진행될수록 커진다 — 그것이 이행의 목적이기도 하다(Lua를 문서 밖으로).

**대안 수용**: 512 KiB로 두되 **초과 시 절차**(앱 단위 상향 신청 등)가 문서에 있으면 그것으로 충분하다.
지금처럼 수치만 있고 경로가 없으면 벽에 닿는 날 배포가 멈춘다.

**수용 기준**: 우리 366.7 KiB가 통과하고, 상한에 닿았을 때 무엇을 하면 되는지가 문서에 있다.

---

### R15 · 모듈 이름 규칙 — P1 (확인)

**요청**: 모듈 이름의 **허용 문자와 길이**를 알려 달라. 특히 `/` 또는 `.`을 쓸 수 있는지.

**필요 이유**: **사이트마다 `00_common.lua`가 있다.** amazon·thumbtack·ebay 셋 다 그 이름이고,
얇은 CONFIG 9개도 원래 그 이름이었다. 레지스트리가 이름으로 키를 잡으므로 **네임스페이스가 필요하다.**

| 방식 | 예 | 필요한 것 |
|---|---|---|
| **선호** | `amazon/00_common`, `thumbtack/00_common` | 이름에 `/` 허용 |
| 대안 | `amazon__00_common` | 제약 없음. 우리 빌더가 평탄화 |

**둘 다 수용한다** — 어느 쪽인지만 알려 주면 빌더가 그에 맞춰 이름을 만든다. 지금은 규칙을 몰라
빌더의 이름 생성 규칙을 정하지 못한다.

부수 질문: 같은 이름 재주입이 live state를 폐기한다고 했는데(§9.2.2), **앱 패키지 배포 시에도
동일한가?** 배포로 모듈이 교체되면 진행 중인 세션의 모듈 상태가 어떻게 되는지가 우리 배포 절차
(무중단 여부)를 좌우한다.

---

## 4. 회신 수용 확인

| 항목 | 수용 |
|---|---|
| **R13** `bind: session_root` 기본값 | 수용. 구현이 프레임 필드 하나로 줄어든 것(서버 3~5번 불필요)이 정확한 분석이다 |
| **R13** SDK 축소분 | **반영한다.** `axsdk-sdk-js` 작업지시서를 "`bindingId`를 싣기" → "`target.bind`를 보고 자기가 루트인지 판정하기"로 고쳐 보낸다 |
| **R8-R `bind: script`** | 계약에만 두고 구현 유예에 동의. `concurrency`가 컴파일 시점에도 강제된다는 확인(`adapters.ts:1065`) 감사 |
| **R12** 엔드포인트 신설 | 수용. **B안 거부 이유(DB 자격증명)에 전적으로 동의한다** — 우리도 앱 하나 배포하려고 전체 테이블 권한을 받고 싶지 않다 |
| **R12** `expectedRevision` | **환영한다.** 요청하지 않았지만 필요하다 — 우리 CI와 수동 배포가 경합할 수 있다 |
| **R12** 롤백/리비전 | 수용. `POST …/package/rollback`이 있으면 배포 사고를 되돌릴 수 있다 |
| **R11** 파일 상한 유지 + 개수 64 + 합계 신설 | 수용. 축을 바꾼 판단이 맞다. 합계 수치만 R14로 논의 |
| **R3-R** | 수용 |

---

## 5. 우선순위 (우리 관점, 갱신)

| ID | 항목 | 우선 | 비고 |
|---|---|:---:|---|
| R11-개수 + R2 1단계 | 개수 64 + 합계 상한 + 모듈 레지스트리 | **P0** | 함께 내는 판단에 동의 |
| **R14** | 합계 512 KiB → 1 MiB 또는 상향 경로 | **P1** | 신규. 개수는 여유, 총량이 한 달 치 |
| **R15** | 모듈 이름 규칙 | **P1** | 신규. 빌더 이름 생성 규칙이 막혀 있다 |
| R13 | `bind: session_root` | P0 | 프레임 필드 하나 |
| R12 | push 엔드포인트 | P0 | 신설 |
| R10 · R3-R | `_ENV` 명세 · gzip | P1 | |
| R1 | `net.fetch` | P0(일정 미정) | 우리는 주입 파라미터로 선행 |

```
Phase 0·1  ────────────────────────  착수 가능 (진행 중)
Phase 2    ──── R2 1단계 · R11-개수 · R15 ────
Phase 3    ──── + R12 ─────────────
Phase 4·5  ──── + R1 · R13 ────────
```

R15가 Phase 2에 걸린 이유: 모듈 이름을 정하지 못하면 빌더를 완성할 수 없다.

## 6. 우리가 하는 일

1. **`axsdk-sdk-js` 지시서를 R13 축소분으로 개정해 보낸다** — 클라이언트 의무가
   "`target.bind` 판정" 하나로 줄었다.
2. `50_commerce.lua` 6분할과 CONFIG 9개 묶기를 Phase 2에서 함께 한다.
3. R15 답변 즉시 빌더의 모듈 이름 생성 규칙을 확정한다.
4. R12 계약 수령 후 `tools/push.mjs` + CI 배포 단계. 그쪽이 먼저 우리 크기로 실증한다고 한 것에 감사한다.
5. 모듈 총량을 배포마다 계측해 상한 대비 사용률을 보고한다 — R14가 512 KiB로 확정되면 특히.

## 7. 회신 필요

| # | 질문 | 막는 것 |
|---|---|---|
| 1 | **R15** 모듈 이름에 `/` 허용 여부 | 빌더 완성 (Phase 2) |
| 2 | **R15-2** 앱 패키지 배포 시 진행 중 세션의 모듈 상태 | 무중단 배포 여부 |
| 3 | **R14** 합계 1 MiB 또는 상향 경로 | 없음 (지금은 통과). 몇 주 뒤 배포가 멈추는 것을 막는 용도 |
