# 회신 (7차) — 5·6차 회신 수용 · 자기 정정 1건 · 신규 보고 1건

| | |
|---|---|
| **발신** | `axsdk-sites` |
| **수신** | backend / runtime |
| **선행** | 5·6차 지시서 → 회신(`configRevision`·R16·`target` 확정) |
| **이 문서** | 수용 확인 + **내 R16 심각도 오판 인정** + §3 폴백 조건 답변 + **신규 보고 R18** |
| **측정** | 2026-07-28, Playground 프로필(9235), `dist/playground` 빌드 |

---

## 0. 요약

**정정 두 건 모두 받는다.** R16이 S1을 막고 있다고 쓴 것은 내 오판이었다 — 서버가 그 헤더를 읽지 않고,
`session_root`는 클라이언트 로컬 판정이다. 실제 차단은 `target` 프레임 필드이고 그쪽 미구현분이다.

**`configHash` 쪽이 낫다.** 리비전이 없는 client-flow-only 앱과 매 턴 오버레이에서 리비전은 그룹 키로
쓸모가 없다는 지적이 맞다. `GET /session` 목록에 실어 주는 것까지 요청보다 많다.

**§3 폴백 조건 — 확인했고, 두 겹으로 방어돼 있다.** 테스트로 고정했다(§2).

**신규 보고 R18** — `opTimeoutMs`가 이동하는 스크립트에서 **비용의 지배 요인**이 된다. 문서 안내와
반대 방향으로 튜닝해야 한다. 우리 파일럿이 260초에서 **29.7초**로 바뀐 원인이다.

**그리고 파일럿이 끝났다** — 사이트 레이어 없이 런타임 Lua가 실제 11번가를 이동·검색해 19건을 읽었다.

---

## 1. 자기 정정 — R16 심각도

내가 6차에 쓴 것: *"클라이언트가 이미 완성한 기능을 서버 한 줄이 막고 있다. R13/S1의 마지막 조각."*

**틀렸다.** 확인 없이 "헤더가 거부되니 그 기능이 못 돈다"로 이었다. 실제로는

- 서버가 `x-axsdk-client-id`를 **읽지 않는다**(`event.ts`·`sse/` 0건).
- `bind: session_root`는 **클라이언트가 자기 바인딩 맵을 보고 판정**한다. 서버는 탭을 알 필요가 없다 —
  3차에서 그렇게 줄인 설계이고, 그 결론을 내가 다음 문서에서 잊었다.

**실제 차단은 `target` 프레임 필드다.** 우선순위를 그쪽 판단대로 받는다: `target`(R13)이 R16보다 먼저다.

**헤더 값 계약은 그대로 지키겠다** — `bindingId`(탭 단위)만 넣는다. `currentAttachmentId`(문서 단위)나
`connId`(연결 단위)는 넣지 않는다. *"지금은 무시하므로 무엇을 넣어도 통과한다"* 는 경고가 정확하다 —
`bind: script`가 붙는 날 문서 단위 id가 들어 있으면 이동마다 바인딩이 끊기고 원인이 안 보인다.

---

## 2. §3 폴백 조건 — 확인 완료, 두 겹 방어

**지적한 함정이 맞다.** `<span class="price"></span>` 는 `type(v) == "string"`이고 `v ~= nil`을 통과한다.
사이트 개편에서 셀렉터는 남고 내용만 비는 것이 흔한 모양이라, 우리 폴백이 그 껍데기에서 멈추면
**뒤 후보를 영영 못 쓴다.**

**확인 결과 우리 체인은 두 겹으로 막고 있다.**

```lua
-- _common/scripts/00_base.lua  B.read_field
value = B.non_empty(raw)                      -- ① 빈 문자열 → nil 로 변환
if value ~= nil and value ~= "" then          -- ② 그래도 한 번 더 본다
```

`B.non_empty`가 `clean_text` 후 `""`이면 `nil`을 돌려주고, 조건절이 다시 `""`를 배제한다.
**한 겹만 무력화해서는 동작이 바뀌지 않는다** — 변이 테스트로 확인했다(둘 다 제거해야 3건이 깨진다).

두 번째 모양도 함께 고정했다. **CSS 셀렉터 목록**(`'a, b, c'`)은 브라우저가 첫 매치를 고르므로
**우리에게 두 번째 기회가 없다.** 그 경우 값을 **거부**한다 — 빈 가격을 0으로 받아들이는 것보다
행을 버리는 편이 맞다(총액 비교에 틀린 숫자가 들어가면 안 된다).

새 테스트: `tools/lua/field-fallback.test.mjs` 4건. `nil`과 `""`를 구분해 주는 계약이 우리에게 맞다 —
**계약을 바꾸지 말아 달라.**

---

## 3. R18 (신규 보고) · `opTimeoutMs`는 이동하는 스크립트에서 지배 요인이다

### 증상

Playground 파일럿(11번가 검색, google.com에서 시작)이 **260초에도 끝나지 않았다.** `deadlineMs: 45000`이
걸려 있었는데도 그랬다.

### 원인 — 측정으로 특정

```
nav.navigate           → settled ok
dom.get_location_href  → received, 응답 없음      (문서가 unload 중)
(다음 폴)               ← +8.5초 = opTimeoutMs 8000 을 꽉 채움
```

**접수하고 죽은 문서는 느린 문서와 구분되지 않는다.** 문서(`AGENTIC_TASKS_RPC_LUA.md` §6.3)는
*"이동 공백에는 연결 문서가 0개라 폴이 `no_client`를 즉시 받는다"* 고 하는데, 그것은 **dispatch 시점에
아무도 없을 때**의 이야기다. 직전까지 살아 있던 문서가 프레임을 받고 unload되면 런타임은 응답을 기다린다.

이동을 여러 번 넘는 스크립트는 폴마다 이 비용을 낼 수 있다.

### 해소 — 우리 쪽 튜닝으로 가능하다

`opTimeoutMs: 8000 → 2000`. 같은 작업이 **29.7초**에 끝났다. 단계별로는:

```
href=712ms  navigate=362ms  wait=4498ms(moved=true)  body=1976ms  total=7548ms
```

이동 자체는 7.5초로 건전하다.

### 요청 — 코드가 아니라 문서

**동작을 바꿔 달라는 것이 아니다.** 우리 쪽 노브로 해소되고, 그 노브가 존재하는 이유가 이것이다.
다만 **저작 가이드의 안내가 반대 방향이다.**

| 현재 문서 | 실제 |
|---|---|
| `opTimeoutMs` — "느린 사이트에서 상향" | **이동하는 스크립트에서는 낮춰야 한다.** 높은 값은 unload 창에 걸린 폴마다 그대로 비용이 된다 |

제안 문구: *"이동하는 스크립트는 `opTimeoutMs`를 낮게(2000 이하) 둔다. 대기의 상한은 헬퍼의 `timeout`이
잡으므로 op 타임아웃은 죽은 문서를 빨리 포기하는 용도다. 높은 값은 느린 사이트가 아니라 **이동**에서
비용이 된다."*

가능하면 §6.3의 "즉시 `no_client`" 문장에 **"dispatch 시점에 연결이 없을 때"** 라는 한정을 덧붙여 달라.
그 문장을 읽고 우리는 폴이 항상 빨리 실패한다고 이해했다.

---

## 4. 수용 확인

| 항목 | 수용 |
|---|---|
| 세션 무효화 안 만듦 | 수용. `stale_resume_node`로 **엔트리로 되돌아간다**는 것은 몰랐다 — 우리 판단(스냅샷이 안전)의 근거가 더 강해졌다. 사용자가 번호를 고르려는데 검색이 처음부터 다시 도는 것은 오류보다 나쁘다 |
| `configHash` 우선 | 수용. 세 용도 전부 해시로 성립한다는 분석이 맞다 |
| `GET /session` 목록에 싣기 | **요청보다 많다.** 한 번 호출로 해시별 세션 수를 세고 롤백 후 0이 되는 것을 같은 호출로 본다 |
| `configRevision`을 R12까지 null 유지 | 수용. 필드를 미리 두어 나중에 우리 코드를 안 고치게 하는 배려에 감사 |
| `target` 계약 확정 | 확인. `target` 없으면 기존 동작이라는 것 포함 |
| R16 우선순위(→ `target` 뒤) | 수용 |
| R17 철회 확인 | 확인 |

---

## 5. 파일럿 결과 (참고)

**Phase 1이 끝났다.** 사이트 레이어 없이 런타임 Lua가 실제 스토어를 이동·검색했다.

```
RPC SEARCH OK · found=19 · cards=24        (google.com 시작, 29.7초)
1. 로지텍코리아 공식 MX Master 4 무선 마우스 그래파이트 · KRW 169000
2. 삼성전자 삼성 무선 마우스 SM-MG100B 멀티페어링 블루투스 · KRW 29900   …
```

- 454줄 durable 체크포인트 상태기계(prepare → navigation_armed → …)가 **직선 스크립트**로 바뀌었다.
- 사이트 CONFIG 9개를 **데이터 모듈 1개**로 묶었다(4차 Q3 답변대로).
- 빌드 산출물 **51.5 KiB = `clientFlows` 상한의 20.1%**.
- 오프라인 테스트: RPC 스텁 하네스 + 유닛 19건. 스텁은 폴 헬퍼·fire-only·op 기록까지 실채널을 모사한다.

**이행 전제가 실제 스토어에서 성립한다.** 남은 것은 규모(Phase 2~5)와 대기 중인 플랫폼 항목뿐이다.

## 6. 남은 것 — 그쪽 §4와 동일

| ID | 상태 |
|---|---|
| R2 1단계 + R11-개수 + R15 | 구현 대기 (P0 묶음) — **Phase 2 착수 조건** |
| **R13 `target` 프레임 필드** | 구현 대기 — **S1의 실제 차단 항목** |
| R12 push + `configRevision` | 구현 대기 |
| R16 allow-list + `configHash` | 구현 대기 (소) |
| R10 · R3-R · R4 · R5 · R6 | 구현 대기 |
| R1 `net.fetch` | 일정 미정 |
| **R18** | **신규 — 문서 문구만** |

## 부록 · 이번 라운드 우리 정정

| 무엇 | 원인 |
|---|---|
| R16 심각도 ("S1을 막고 있다") | 서버가 헤더를 읽지 않는다는 것을 확인하지 않고, 3차에서 합의한 "클라이언트 로컬 판정"을 잊었다 |
| 파일럿 정지를 "탐색 경로 미해결"로 보고 | 원인은 우리 `opTimeoutMs` 설정이었다. 단계별 측정 전에 플랫폼을 의심했다 |
