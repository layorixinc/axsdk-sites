# RPC Lua 런타임 요청 20 — 요청 18·19 종결. 전 구간 라이브 통과, 요청 없음

날짜: 2026-08-23 · 대상: agentv3 runtime · 작성: axsdk-sites
근거: 라이브 실측, 픽스처 페이지 단일 세션. `check:flows` 188 · `test:lua` 580 · extension 1029 · core 800.
관련: 요청 18(질문) · 19(측정) · 회신 커밋 `5e4022e`(runtime) · `81272f2`(docs)

**요청 없음.** `contextAccess`로 전 구간이 동작합니다. 우회로(요청 19 §4의 D안)는 만들지 않았습니다.

---

## 1. 두 가지가 겹쳤고, 둘 다 정확히 지적하신 대로였습니다

`flowToolAdapter`가 `contextAccess`를 빠뜨렸다는 것 — 앱 작성자가 쓸 수 있는 유일한 표면에서
동작하지 않았으니 기능이 없었던 것과 같다는 판단에 동의합니다. 그리고 **선언이 두 개 필요하다**는
정정이 저희가 놓친 절반이었습니다. 요청 18을 절반만 적어 보낸 셈입니다.

전역 이름도 확인했습니다 — `contexts` 테이블의 키입니다. 선택자가 쓰는 그대로입니다.

```yaml
# 노드
inputSelector: [ contexts.community, named_command, requestText, userMessages ]
# 도구
contextAccess:
  read: [ community ]
```

## 2. 게이트 제안, 정정까지 반영했습니다

18차의 "`parameters`가 아니라"에서 "아니라"가 틀렸다는 정정 그대로 세 조건으로 썼습니다:
`parameters`에 있으면 안 되고, `contextAccess`에 있어야 하고, `inputSelector`에도 있어야 합니다.
**반대 방향도 넣었습니다** — 어떤 노드도 선택하지 않는 컨텍스트를 도구가 읽겠다고 선언하면 빈 뷰를
받고 조용히 아무것도 읽지 못하므로, 그것도 실패로 잡습니다.

기존 게이트("계약 노드가 선택한 필드는 도구가 선언해야 한다")는 `contexts.*`를 **예외**로
두었습니다. 둘이 같은 줄에 정반대를 요구하고 있었습니다.

## 3. 저희 쪽에서 하나 더 고쳐야 했습니다 — 카탈로그가 스크립트를 지목하지 못했습니다

브로커는 `scriptId`로 디스패치하는데, 카탈로그는 표시 이름만 렌더링했습니다
(`- <name> <version> (<publisher>, reviewed by <reviewer>)`). 그래서 모델이든 결정적 노드든
**어느 스크립트인지 말할 수단이 없었고**, 표시 이름은 두 설치가 공유할 수 있습니다. 엔트리 줄에
백틱으로 id를 넣었습니다. capability 토큰도 digest도 소스 URL도 아티팩트도 아닌, 사용자가 설치한
것의 이름입니다.

카탈로그를 읽을 수 있게 되니 **모델에게 알 수 없는 사실을 묻지 않게 되었습니다.** `propose`는
`script_id`/`version`/`effect`를 인자로 받아 검증했는데, 라이브에서 모델이 어휘 밖의 effect를
답해 `effect_invalid`로 죽었습니다 — 그렇게 끝나는 것 말고는 길이 없습니다. 지금은 `command`와
`arguments_json`만 받고 나머지는 명령 이름으로 조회합니다. 한 사실에 필자가 둘이면 운으로 맞거나
틀립니다.

## 4. 라이브, 전 구간 (픽스처 페이지, 한 세션)

| 단계 | 증거 |
|---|---|
| 결정적 분류 | `{"next":"propose","named_command":"remember"}` |
| 카탈로그에서 파생 | `script_id: fixture.read-page`, `version: 1.0.0`, effect는 그 줄에서 |
| 문장에서 인자 | `{"note":"clicked proof"}` — 사용자가 실제로 말한 값 |
| 실제 브라우저 클릭 | isolated world 클릭 `{"clicked":true,"tag":"BUTTON"}` |
| 효과 | `recall` → `{"ok":true,"value":{"note":"clicked proof"}}` |
| 답변 경로 (회귀) | "이 페이지에는 **AXSDK community fixture page** 라고 쓰여 있습니다" |

효과는 **브로커를 통해 되읽어** 확인했습니다. 도구가 스스로 보고하는 성공 플래그는 자기 자신에 대한
주장이지 효과의 증거가 아니라는 것이 이 저장소의 규칙입니다.

## 5. 참고 — 하네스로 위젯을 클릭하려면 이벤트가 필요합니다

`widget.js`는 `world: 'ISOLATED'`로 주입되므로 페이지 월드 `evaluate`는 버튼을 **하나도** 찾지
못합니다. 버튼을 표시하고 있는 바로 그 탭에서 0을 반환했습니다. 실행 컨텍스트를 열거해
`AXSDK Assistant (CDP)` 이름의 월드에서 클릭해야 닿습니다.

`browser-session.mjs`의 CDP 클라이언트는 `send`만 노출하고 이벤트는 버립니다
(`Runtime.executionContextCreated`를 받을 방법이 없습니다). 그래서 이번 클릭 증명은 이벤트를
노출하는 다른 클라이언트로 했습니다. **요청은 아닙니다** — 그쪽 파일이고, 필요하면 저희가 쓰는
쪽에서 처리하면 됩니다. 다만 위젯을 라이브로 클릭하려는 다음 사람이 같은 자리에서 멈출 것이라
적어둡니다.

## 6. 요청 18-2 관련

발행 요청은 철회한 그대로 두겠습니다. 다만 클라이언트 구현도 **하지 않기로** 했습니다 — 배포
문제가 아니라 설계 판단입니다. **확인 버튼이 동의 표면**이고, 모델이 명령을 직접 호출하면 그것이
사라집니다. `read` 한 건에서 턴 하나를 아끼고 나머지 전부에서 게이트를 내주는 거래입니다. 플로우는
`community.*` op을 grant하지 않고 `check:flows`가 그것을 고정합니다.

## 7. 검증

| | |
|---|---|
| 게이트 | `check:flows` **188** · `test:lua` **580** · extension **1029** · core **800** · `check:bundle` ok(32 assets) · 정책·레지스트리 통과 |
| 뮤테이션 | 4건 — 카탈로그보다 `args.effect`를 신뢰(**처음엔 살아남음**: 충돌하는 값을 주는 테스트가 없었고, 그게 바로 이 변경의 요지였습니다. 단정문을 추가했습니다), 모호한 명령 수용, 카탈로그 줄에서 id 제거, classify 도구에서 `contextAccess` 제거 |
| 회귀 | 답변 경로를 변경마다 재확인 |

회신 감사합니다. 두 번 다 저희가 보낸 절반짜리 질문에서 나머지 절반을 짚어주셨습니다.
