# 작업지시서 (3차) — 2차 회신에 대한 답변 · 정정 1건 · 신규 요청 2건

| | |
|---|---|
| **발신** | `axsdk-sites` |
| **수신** | backend / runtime |
| **선행** | 1차 지시서 → 1차 회신 → 2차 지시서 → **2차 회신 (R8-R·R9·R10·R11·R3-R·R1-C·R2-C)** |
| **이 문서** | 회신 §4 질문 4건 답변 + **R11 전제 정정** + 신규 요청 2건 |
| **측정 시점** | 2026-07-28, `main` @ `b8e5e8b` |

---

## 0. 요약

**R11의 전제가 틀렸다 — 내 자료 표기 때문이다.** 64 KiB를 넘는 파일은 **3개가 아니라 1개**이고,
그 1개도 분할로 해소된다. **상향이 필요 없다**(§2).

**Q3(탭 식별자) 확정 — 이미 존재한다.** 확장이 `chrome.storage.session["axsdk:tab-bindings:v2"]`에
탭 단위 바인딩을 유지하고 있고, 거기 **이동을 넘어 안정한 식별자**가 이미 들어 있다(§1.3).
R8-R의 클라이언트 의무는 "새로 만든다"가 아니라 "있는 것을 노출한다"로 줄어든다.

**신규 요청 2건**
- **R12** — A안 실행에 필요한 **push 경로 계약**. 자격은 있는데 도구가 없다.
- **R13** — 스티키 바인딩의 **기본값을 "첫 응답 탭"이 아니라 "세션 루트 탭"으로.** 첫 응답 방식은
  사용자의 무관한 탭(메일·문서)을 붙잡아 쇼핑몰로 **이동시킨다.**

**수용**: R9 정정(차단 아님, 여유 문제) · R3-R 근거 정정(시간 벌기 아님) · R10(공유, 우리 무변경) ·
매 턴 교체는 개발 전용.

---

## 1. 회신 §4 질문 답변

### Q1 · A안(앱 패키지 배포)을 택하는가 — **택한다. 다만 도구가 없다**

**택한다.** 근거는 그쪽이 실행으로 확인해 준 것 그대로다 — 앱 base `flowDocument`에는 크기 검사가 없고
push 상한이 512 KiB다. 우리 190.5 KiB 본체는 여유가 있다.

**전달 모델 이해를 아래로 확정한다.**

| | 무엇을 | 어디로 |
|---|---|---|
| 프로덕션 | 플로우 본체 + 공유 Lua 모듈 | **앱 패키지 (push)** |
| 개발 | 변경분 오버레이 | `clientFlows` (매 턴 교체) |
| 프로덕션 세션 | — | `clientFlows` 미사용, 또는 세션 생성 1회 |

**매 턴 교체를 프로덕션에 쓰지 않겠다.** 비용 사슬(yaml.load + 전체 검증 + 세션 record 재직렬화 + HSET +
`waitForDurability` + promptAsync 본문 재전송)을 명시해 준 것이 판단 근거가 됐다. 1차 회신 Q2만 봤을
때는 "매 턴 가능"을 개발·프로덕션 공통으로 읽었다.

**막히는 지점: push 도구가 우리 저장소에 없다.** 자격은 있다 — `.env`에 `AXSDK_APP_ID` / `AXSDK_API_KEY`가
있고 개발 하네스가 이미 그것으로 백엔드와 통신한다. 없는 것은 **배포 경로 자체**다.
우리 `tools/`에는 `ax.mjs`(개발 하네스) · `playground.mjs` · `merge-lua.mjs`(번들러) · 테스트뿐이고,
그쪽 `agentv2_push.ts` 상당물이 없다. → **R12**.

### Q2 · `flow.map` 10개 사이트: 탭 10개 병렬인가, 1개 직렬인가 — **1개 탭 직렬. 선택이 아니라 강제다**

```yaml
# _common/flows.yaml — 두 map 모두
maxItems: 3   / 10
concurrency: 1
onItemError: collect
```

`FLOWS.md` §9.17이 **V1에서 `concurrency`는 1로 고정**이라고 규정한다("must be 1 in V1", "items still run
one at a time"). 우리가 1을 고른 게 아니라 플랫폼이 1만 허용한다.

**따라서 그쪽이 우려한 "10개 실행이 한 탭을 짓밟는 경합"은 지금 구조에서 발생하지 않는다.**
배타성 요구는 여전히 타당하지만 **긴급하지 않다** — 필요해지는 것은 V2에서 `concurrency > 1`이 열릴 때다.
그때 조용히 깨지지 않도록 **지금 계약에 넣어 두는 것**에는 찬성한다.

**다만 우리가 실제로 겪을 위험은 다른 것이다 → Q3 답변 끝과 R13.**

### Q3 · 이동을 넘어 안정한 탭 식별자를 확장이 낼 수 있는가 — **낼 수 있다. 이미 내고 있다**

라이브 프로필의 확장 서비스워커에서 읽은 실제 값이다.

```jsonc
// chrome.storage.session["axsdk:tab-bindings:v2"]
{
  "bindings": {
    "365472117": {                                   // ← chrome tabId 가 키
      "tabId": 365472117,                            // ← 이동을 넘어 안정
      "bindingId": "c4bc44a5-65f9-44e0-a74f-1ff3c73fcfef",       // ← 이동을 넘어 안정
      "currentAttachmentId": "71ed131f-d8c4-4acd-b31b-c442ae83c178",  // ← 문서 단위, 이동하면 바뀜
      "kind": "root",
      "sessionOwnerBindingId": "c4bc44a5-65f9-44e0-a74f-1ff3c73fcfef"
    }
  },
  "owners": { "c4bc44a5-…": { "sessionId": "ses_fa6b4d966001T9kitD2vtnj65v" } },
  "revision": 5, "version": 2
}
```

식별자 층이 **이미 셋으로 분리되어 있다.**

| 식별자 | 수명 | 그쪽 §2 표의 무엇에 해당하나 |
|---|---|---|
| `connId` | 연결 단위 | 재연결하면 새로 발급 — 쓸 수 없다(그쪽 판단대로) |
| `currentAttachmentId` | **문서** 단위 | 이동하면 바뀐다 |
| **`tabId` / `bindingId`** | **탭** 단위 | **이동을 넘어 안정. 이것이 답이다** |

- `chrome.tabs`는 서비스워커에서 사용 가능하다(`hasTabsApi: true`, `canQueryTabs: true`). 매니페스트
  권한은 `storage` + `webNavigation`뿐이지만 **`tabs` 권한 없이도 `sender.tab.id`는 항상 온다** —
  이미 그렇게 채워지고 있다.
- `sessionStorage`를 쓰지 말라는 지적은 맞고, 애초에 쓰고 있지 않다.

**단, 이 작업의 소유자는 우리가 아니다.** 확장은 `axsdk-sdk-js`(별도 저장소)에 있고 `axsdk-sites`는
flow와 Lua만 소유한다. 그쪽 §2 구현 표의 **1번(클라이언트가 SSE 구독 시 탭 식별자를 싣는다)** 은
`axsdk-sdk-js` 과제다. 다행히 범위는 **"새 메커니즘 구현"이 아니라 "이미 있는 `bindingId`를 구독과 RPC
응답에 싣기"** 로 줄어든다. 그 사실을 근거와 함께 SDK 쪽에 전달하겠다.

**순수 웹 SDK 클라이언트는 이 기능을 못 쓴다**는 계약 명시에 동의한다.

### Q4 · 세 파일을 128 KiB 안에 유지할 수 있는가 — **질문의 전제가 틀렸다. §2 참조**

---

## 2. 정정 — R11의 근거가 된 내 표기

**내 2차 §1.2 표가 오독을 유발했다.** "사이트 Lua 합계 / amazon 65.2 / thumbtack 76.3"은
**디렉터리 합계**였는데 파일 크기로 읽혔다. 명시하지 않은 내 잘못이다.

**실제 파일 단위 측정** — 모듈은 파일 단위이므로 이것이 상한에 걸리는 값이다.

| 대상 | 파일 수 | **최대 단일 파일** | 합계 | 64 KiB |
|---|---:|---:|---:|---|
| `_common/scripts/50_commerce.lua` | 1 | **75.5 KiB** | 75.5 KiB | **초과 — 유일** |
| `thumbtack/scripts/` | 8 | **47.0 KiB** (`00_common.lua`) | 76.3 KiB | 통과 |
| `amazon/scripts/` | 8 | **38.8 KiB** (`00_common.lua`) | 65.2 KiB | 통과 |
| `_common` 나머지 10개 | 10 | 33.0 KiB (`60_storefront`) | 119.8 KiB | 통과 |

**64 KiB를 넘는 파일은 `50_commerce.lua` 하나뿐이고, 그것도 분할로 해소된다.**

```
50_commerce.lua 75.5 KiB → 5개 모듈
  50_commerce_core      (헤더·유틸·FX·관련도)      22.2 KiB
  51_identity           (prepare/lock/options/resolve) 11.3 KiB
  52_verify             (verify_product_offers)        17.8 KiB
  53_screening          (build/apply_offer_screening)   3.5 KiB
  54_comparison         (rank/present/refine/resolve)  14.1 KiB
  55_store_io           (normalize/collect/search/cart) 6.6 KiB
```

R10에서 `_ENV` 공유가 확정됐으므로 **분할 비용이 거의 없다** — 파일을 자르고 `execute.modules` 순서만
맞추면 기존 교차 참조가 그대로 동작한다. 어차피 1,938줄 파일은 리뷰 단위로도 너무 크다.

**따라서 R11 상향은 우리 때문에 필요하지 않다.**

- 올리겠다면 **여유분으로** 받겠다. 컴파일 비용이 선형이고 캐시된다는 실측도 봤다.
- 올리지 않아도 우리는 진행한다. **모듈당 48 KiB 이하를 우리 규율로 삼겠다** — 그보다 큰 모듈은
  상한 이전에 리뷰가 안 된다.
- **합계 상한**을 두는 편이 낫다는 판단에 동의한다. 우리 합계는 190.7 KiB이고, 분할 후에도 같다.

**성장 근거**(상향을 여유분으로만 봐야 하는 이유): `50_commerce.lua`는 2026-07 한 달에 **순증 +1,937줄**
이다. 사실상 이번 분기에 전부 자랐다. 상한 상향에 기대는 것은 같은 벽을 몇 달 뒤에 다시 만나는 선택이다.

---

## 3. 신규 요청

### R12 · 앱 패키지 push 경로 계약 — **P0, A안 실행 전제**

**요청**: `axsdk-sites`가 플로우 본체와 Lua 모듈을 앱 패키지로 배포할 수 있는 경로. 아래 중 하나.

| | 방법 | 우리 선호 |
|---|---|---|
| **A** | HTTP 엔드포인트 계약 공개 (`POST …/apps/:id/package` 형태) — 우리가 `tools/push.mjs`를 만든다 | **선호.** CI에 붙이기 쉽다 |
| **B** | `agentv2_push.ts` 상당 CLI를 배포물로 제공 | 가능 |

**요구 사항**
1. 인증은 기존 `x-api-key` + `x-app-id`(우리 `.env`에 이미 있다).
2. **원자적 교체.** 부분 반영된 상태로 사용자 요청을 받는 창이 없어야 한다 —
   Q5(문서 전체 무효화)와 같은 이유다.
3. **거부 사유가 수치로.** 512 KiB 초과 시 `{ limit, actual }`.
4. **롤백 또는 직전 버전 확인** 수단. 배포 후 전 인텐트가 죽는 사고를 되돌릴 방법이 필요하다.
5. 배포 결과에 **적용된 문서의 해시**. 우리 CI가 "밀어넣은 것 = 도는 것"을 확인해야 한다.

**수용 기준**: 우리 190.5 KiB 본체 + 모듈 190.7 KiB를 CI에서 1회 배포하고, 반환 해시가 로컬 산출물과
일치한다.

**미제공 시**: A안을 택했는데 배포할 수단이 없다. Phase 3 산출물이 저장소에만 남는다.

---

### R13 · 스티키 바인딩의 기본값 = **세션 루트 탭** — **P0, R8-R 하위**

**요청**: 바인딩 대상 선정 기본값을 "첫 응답 탭"이 아니라 **세션을 소유한 루트 탭**으로.

**근거 — "첫 응답"은 사용자의 탭을 파괴한다.** 우리 스크립트의 첫 op은 `dom.get_location_href()`이고
**모든 탭이 답할 수 있다.** 사용자가 메일·문서·은행 탭을 열어 둔 상태라면 그중 하나가 먼저 답할 수 있고,
바인딩된 그 탭을 스크립트가 **곧바로 쇼핑몰로 이동시킨다.** 읽기 op의 경합은 무해하지만
**바인딩은 이동 권한을 주는 행위**라 성질이 다르다.

**확장은 이미 답을 갖고 있다.** §1.3의 바인딩 맵에 `kind: "root"` 와 `sessionOwnerBindingId`가 있고,
`owners`가 `bindingId → sessionId`를 들고 있다. **세션이 어느 탭의 것인지 이미 안다.**

**제안**

```yaml
rpc:
  target:
    bind: session_root     # 기본값: 세션 소유 탭. 첫 응답 경합 없음
    # bind: script         # 첫 응답 탭 (다중 탭 작업용, 명시적으로만)
    urlPattern: "..."      # 선택: script 모드에서 후보 좁히기
```

- `session_root`가 **기본값**이어야 한다. 위험한 쪽을 명시적 선택으로 만든다.
- 루트 탭이 응답하지 않으면(닫힘) `no_client`. 다른 탭으로 넘어가지 않는다.
- 배타성(그쪽 요구)은 `bind: script`에서만 의미가 있다 — `session_root`는 정의상 하나다.

**수용 기준**
- 탭 3개(루트 = 쇼핑 탭, 그 외 2개)에서 스크립트는 **항상 루트 탭에서만** 실행된다.
- 루트 탭을 스크립트가 다른 사이트로 이동시켜도 바인딩이 유지된다.
- 루트 탭이 닫히면 다음 op은 즉시 `no_client`이고, 무관한 탭이 대신 실행하지 않는다.

**미제공 시**: 사용자의 무관한 탭이 이동될 수 있다. 우리는 그 위험을 안고 Phase 4를 출시할 수 없다.

---

## 4. 회신 수용 확인

| 항목 | 수용 |
|---|---|
| **R9 정정** | 수용. 190.5 KiB는 지금 통과하고 "차단"이 아니라 **여유** 문제다. 내 2차 문서의 "Phase 2 이후 차단"은 과장이었다 |
| **R9 A안** | 채택 (§Q1). 실행으로 확인해 준 것(오버레이 300 KiB 거부 / 앱 base 300 KiB 통과)이 결정적이었다 |
| **R3-R 근거 정정** | 수용. gzip은 **해제 후 바이트**에 걸리므로 상한과 무관하다. "R9를 논의하는 동안 시간을 번다"는 내 근거는 틀렸다. **개발 루프 이득(190 KiB → 약 40 KiB)만으로 우선순위를 판단해 달라** — 우리에겐 그것도 충분히 크다 |
| **R10** | 수용. `_ENV` 공유 + state당 1회 평가. **우리 쪽 작업이 사라졌다** — `return M` 11건, 교차 참조 78건 정리 모두 불필요 |
| **R10 주의사항** | 인지. 모듈 최상위 부수효과는 세션당 1회다. 최상위 RPC 호출을 하지 않겠다. 캐시 초기화도 최상위에 두지 않겠다 |
| **R8-R 배타성** | 타당하나 **지금은 발생 불가**(§Q2, `concurrency` 1 고정). V2 대비로 계약에 넣는 것에 찬성 |
| **R1-C 순서** | 수용. R1을 뒤에 두는 이유(인프라 승인이 나머지를 막지 않게)에 동의. **환율을 주입 파라미터로 두고 Phase 4 로직을 먼저 완성**하라는 제안 채택 |
| **R2-C** | 수용. A안을 택하므로 2단계 가치가 준다는 판단에 동의 — **P1 → P2로 내려도 된다** |
| **1.4 `fields` 정본화** | 확인. "매치 없으면 키 없음"이 계약에 포함된 것이 핵심이다 |

---

## 5. 우선순위 (우리 관점)

| ID | 항목 | 우선 | 상태 |
|---|---|:---:|---|
| **R12** | push 경로 계약 | **P0** | 신규. A안 실행 전제 |
| **R13** | 바인딩 기본값 = 세션 루트 탭 | **P0** | 신규. R8-R 하위 |
| **R8-R** | 스티키 탭 바인딩 | **P0** | 수용됨. 클라이언트 의무는 `axsdk-sdk-js` |
| R2 1단계 | 모듈 레지스트리 + `execute.modules` | **P0** | Phase 2 착수 조건 |
| R10 | `_ENV` 명세 + 테스트 | P1 | **해결** (공유 확정) |
| R3-R | gzip | P1 | 개발 루프 이득으로만 |
| R11 | 모듈 상한 | **P2** | **우리 때문에는 불필요** (§2) |
| R1 | `net.fetch` | P0(일정 미정) | 인프라 승인 대기. 우리는 주입 파라미터로 선행 |
| R4/R5/R6 | 관측성·버전·폴 예산 | P1 | 수용 완료 |

```
Phase 0 (기반)         ─────────────────  착수 가능
Phase 1 (Playground)   ─────────────────  착수 가능
Phase 2 (순수 모듈)    ──── R2 1단계 ────   (R10 해결로 우리 선행작업 소멸)
Phase 3 (스토어프론트) ──── R2 · R12 ─────
Phase 4 (비교)         ──── R1 · R8-R · R13 ────
Phase 5 (견적)         ──── R1 · R8-R · R13 ────
```

## 6. 우리가 하는 일

1. **Phase 0~1 착수** — 차단 없음.
2. **`50_commerce.lua` 분할**(§2, 6개 모듈) — R11 결정과 무관하게 진행한다. 모듈당 48 KiB 규율.
3. **`axsdk-sdk-js`에 R8-R 클라이언트 의무 전달** — `bindingId`를 SSE 구독과 RPC 응답에 싣는 건.
   §1.3의 실측 바인딩 맵을 근거로 첨부한다.
4. Phase 4 비교 로직을 **환율 주입 파라미터**로 선행 구현 — `net.fetch` 도착 시 한 줄 교체.
5. R12 계약 수령 즉시 `tools/push.mjs` + CI 배포 단계.
6. 각 항목은 그쪽 수용 기준을 그대로 시나리오 테스트로 만든다.

## 7. 회신 필요

| # | 질문 | 막는 것 |
|---|---|---|
| 1 | **R12 push 경로** — A/B 중 무엇으로, 계약은? | Phase 3 산출물 배포 |
| 2 | **R13 기본값** — `session_root`를 기본으로 할 수 있는가? | Phase 4 출시 가능 여부 |
| 3 | R11을 올릴 것인가 — 올리면 여유분으로 받고, 안 올려도 우리는 분할로 진행한다 | 없음 (정보) |
