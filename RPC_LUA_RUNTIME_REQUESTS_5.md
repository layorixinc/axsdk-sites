# 회신 확인 (5차) — 세션 무효화 질문 답변 · 합의 확정

| | |
|---|---|
| **발신** | `axsdk-sites` |
| **수신** | backend / runtime |
| **선행** | 1~4차 지시서 / 1~4차 회신 |
| **이 문서** | 4차 회신 §6의 질문 **1건 답변** + 확정된 제약 정리 |
| **신규 요청** | **없음.** 이 라운드로 닫는다 |
| **측정 시점** | 2026-07-28 |

---

## 1. §6 질문 답변 — "롤백이 새 세션에만 듣는 것"이 문제인가

### **지금은 문제가 아니다. 세션 무효화를 만들지 마라.**

이유 세 가지.

**(1) 스냅샷이 우리에게 오히려 안전하다.** 우리 플로우는 **여러 턴에 걸쳐 노드에서 멈춰 있는다** —
비교 윈도우에서 번호 선택을 기다리고, 견적 승인 게이트에서 사용자 확인을 기다린다. 세션 중간에 문서가
바뀌면 **일시정지된 플로우가 존재하지 않는 노드로 재개**될 수 있다. 그건 Q5에서 합의한
"문서 전체 무효화"와 같은 부류인데, **조용히** 일어나는 쪽이라 더 나쁘다. 배포가 진행 중 세션을 건드리지
않는 편이 맞다.

**(2) 세션 수명에 구조적 상한이 있다.** 세션 소유권 맵(`axsdk:tab-bindings:v2` → `owners[].sessionId`)이
**`chrome.storage.session`에 있다**(실측). 이 저장소는 브라우저 종료 시 비워지므로 **세션은 브라우저
실행 1회를 넘지 못한다**`[추정: 확장 구현이 재수립하지 않는다는 전제]`.

**(3) 실제 회전이 빠르다.** 개발 프로필 1개월 사용 기준 **바인딩(=세션) 44개**가 생겼다 —
탭 생성·브라우저 재시작마다 새로 생긴다. 잘못된 배포의 노출 창은 "영원"이 아니라 **그 순간 열려 있는
탭들의 남은 수명**이다.

**(4) 아직 프로덕션 사용자가 없다.** 사이트 레이어가 `origin/main`에 푸시되지 않아 실사용자 노출이 0이다.
이 문제가 실제 비용이 되는 시점은 이행 완료 이후다.

### 대신 필요한 것 — 작은 관측 하나

세션 무효화라는 **subsystem은 필요 없지만**, 잘못된 배포가 났을 때 **누가 영향받는지 알 수단**은 필요하다.
지금은 그것조차 없다.

**요청(신규 아님, R12 응답 필드의 확장)**: 세션이 **어느 리비전을 스냅샷했는지** 노출해 달라.

```jsonc
GET <runtime>/session/<sessionID>/state
→ { ..., "configRevision": 8 }        // 이 세션이 물고 있는 앱 패키지 리비전
```

이것만 있으면 우리가 할 수 있는 일:
- 사고 발생 시 **영향 세션 수를 센다**(리비전 8을 물고 있는 세션이 몇 개인가).
- 사용자에게 **"탭을 새로 여시면 해결됩니다"** 라고 정확히 안내한다. 지금은 그 말을 할 근거가 없다.
- 롤백 후 노출이 **언제 0이 되는지** 관측한다.

**세션을 강제로 재구성하는 기능은 필요해지면 그때 요청하겠다.** 판단 근거는 위 관측치가 쌓인 뒤에 갖는
편이 낫다.

---

## 2. 확정된 제약 — 우리 쪽 기록

이 이행에서 우리가 지켜야 하는 값이다. 우리 설계 문서(`RPC_LUA_MIGRATION.md`)에 반영했다.

| 제약 | 값 | 우리 현재 | 여유 |
|---|---:|---:|---|
| 모듈당 바이트 | 64 KiB | 최대 47.0 KiB (`thumbtack/00_common`) | 우리 규율 48 KiB |
| 모듈 개수 | 64 | **32** | 전용 어댑터 4개분 |
| 앱 패키지 `luaModules` 합계 | **2 MiB** | **366.7 KiB** | 267 KiB/월로 6.3개월 |
| 세션 레지스트리 합계 | 512 KiB | 개발 중 교체분만 | — |
| `clientFlows` 오버레이 | 256 KiB | 개발용 오버레이만 | — |
| 앱 패키지 문서 (각각) | 512 KiB | 본체 **190.5 KiB** | 여유 |
| 모듈 이름 | `^[A-Za-z0-9_][A-Za-z0-9_.\-]*$`, 128자, 구분자 `.` | `amazon.00_common` 형태로 생성 | — |

**신뢰 경계로 상한을 나눈 판단이 맞다.** 앱 패키지(배포·검토됨)와 세션 레지스트리(무검토, 동시 세션에
곱해짐)는 다른 자원이다. 우리가 1 MiB 단일 상한을 요청한 것보다 나은 답이다 — 프로덕션은 2 MiB를 쓰고
개발 중 업로드만 512 KiB에 묶인다.

**빌더 이름 규칙 확정**: `amazon/scripts/00_common.lua` → `amazon.00_common`,
`_common/scripts/50_commerce_core.lua` → `_common.50_commerce_core`. 저작은 디렉터리 구조 그대로 두고
평탄화만 한다. `__` 대신 `.`을 쓴다(정본 준수).

---

## 3. 수용 확인

| 항목 | 확인 |
|---|---|
| **R14** 2 MiB / 512 KiB 분리 + per-app override + 배포 응답 사용률 | 수용. **사용률을 배포 응답에서 받는 형태가 우리가 세는 것보다 낫다** |
| **R15** `.` 구분자 · 패턴 · 128자 | 수용. 빌더 이름 생성 규칙을 이 값으로 확정했다 |
| **R15** 이름 검증 부재(도달 불가 모듈) 수정 | 감사하다. 우리가 `amazon/00_common`으로 주입했으면 조용히 당했을 결함이다 |
| **R15-2** 배포 무중단 + 롤백의 단방향성 | 수용. §1이 답변이다 |
| **Q3 정정** | 확인. `contexts` 제안 철회를 받는다 — 우리도 개수만 보고 판단할 뻔했다 |
| **R12** 실증 크기 190.7 → **366.7 KiB** 정정 | 맞다. 모듈 전체(사이트 포함)가 366.7 KiB다 |
| **R2 2단계** P2 | 동의 |

---

## 4. 이 이행의 미해결 항목 (전체)

우리 쪽 관점에서 남은 것은 **구현 대기 4건과 일정 미정 1건**뿐이다. **설계 논의는 닫혔다.**

| ID | 상태 | 막는 것 |
|---|---|---|
| R2 1단계 + R11-개수 + R15 | 구현 대기 (P0 묶음) | **Phase 2** |
| R13 `bind: session_root` | 구현 대기 | Phase 4 출시 |
| R12 push 엔드포인트 | 구현 대기 (신설) | Phase 3 산출물 배포 |
| R10 · R3-R · R4 · R5 · R6 | 구현 대기 | — |
| R1 `net.fetch` | **일정 미정** (인프라 승인) | Phase 4~5. 우리는 주입 파라미터로 선행 |

**우리가 지금 하는 일**: Phase 0(기반) · Phase 1(Playground 파일럿)은 차단 없이 진행 중이다.
`50_commerce.lua` 6분할과 CONFIG 9개 묶기는 Phase 2에서 R2 1단계 도착과 함께 한다.

---

## 5. 이 라운드 요약

**신규 요청 없음.** §1의 `configRevision` 하나만 R12 응답 필드에 얹어 주면 된다 —
새 subsystem이 아니라 이미 있는 값의 노출이다.

4라운드 동안 양쪽이 정정한 것을 남겨 둔다. 다음에 같은 실수를 반복하지 않기 위해서다.

| 누가 | 무엇 | 원인 |
|---|---|---|
| runtime | R11 근거 (파일 3개 → 1개) | 우리 디렉터리 합계표를 파일 크기로 확장 |
| runtime | R8-R 기본값 (첫 응답 → 세션 루트) | 바인딩이 이동 권한을 준다는 점 미고려 |
| runtime | R12 성격 (계약 공개 → 신설) | push가 HTTP라고 암시, 실제로는 DB 직접 |
| runtime | Q3 조언 (`contexts` → 모듈 1개) | 개수만 보고 더 비싼 자원(문서 바이트)을 쓰라고 함 |
| **axsdk-sites** | R3-R 근거 ("gzip이 시간을 번다") | gzip이 해제 후 바이트에 걸린다는 것을 모름 |
| **axsdk-sites** | R9 심각도 ("Phase 2 이후 차단") | 190.5 KiB가 이미 통과한다는 것을 확인하지 않음 |
| **axsdk-sites** | 성장 근거 (+1,937줄/월) | 파일 **생성**을 성장률로 제시 |
| **axsdk-sites** | §1.2 표기 | 디렉터리 합계를 파일 크기로 읽히게 적음 (runtime R11 오류의 원인) |

정정이 양쪽에서 나왔고 전부 **측정으로** 잡혔다. 다음 라운드도 같은 기준으로 하자.
