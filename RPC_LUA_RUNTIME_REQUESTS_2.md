# 작업지시서 (2차) — RPC Lua 이행: 잔여 요청

| | |
|---|---|
| **발신** | `axsdk-sites` |
| **수신** | backend / runtime |
| **선행** | 1차 작업지시서 `RPC_LUA_RUNTIME_REQUESTS.md` → runtime 회신 (R1~R8, Q1~Q5) |
| **이 문서** | 회신에서 요구한 **입력 자료 5건 제공** + **신규·개정 요청 6건** |
| **측정 시점** | 2026-07-28, `axsdk-sites` `main` @ `b8e5e8b` |
| **관련 설계** | `RPC_LUA_MIGRATION.md` (회신 반영해 갱신 완료) |

---

## 0. 1차 이후 달라진 것

| ID | 1차 | 회신 결과 | 2차 |
|---|---|---|---|
| R1 | 아웃바운드 HTTP P0 | **설계 확정** — `net.fetch` 호스트 프리미티브 | 착수·일정 회신 요청 (**R1-C**) |
| R2 | 모듈 재사용 P0 | **설계 변경 수용** — 문서 밖 레지스트리 + 개별 청크 | 2단계 근거 제공 (**R2-C**) |
| R3 | 상한 공개 P1 | 256 KiB 이미 존재, 상향 없음, gzip 신규 | **우선순위 상향 요청 (R3-R)** |
| R4 | 관측성 P1 | 수용 (observer 배선) | 확인만 |
| R5 | op 버전 P1 | 수용 + `types` | **`fields` 계약 정의 제출 (§1.4)** |
| R6 | 폴 예산 P2 | 수용 — 폴은 `maxCalls` 미소모 | 확인만 |
| R7 | `page.eval` P2 | 서버 게이트 없음, 권고 "쓰지 마라" | **철회 — 쓰지 않는다** |
| R8 | — (그쪽 신규) | `target: { urlPattern }` | **개정 요청 (R8-R)** — 그대로는 사용 불가 |
| R9 | — | — | **신규 P0** — 모듈을 다 빼도 문서가 74% |
| R10 | — | — | **신규 P0(소)** — 모듈 청크 `_ENV` 계약 미정의 |

**차단 구조가 바뀌었다.** Phase 4를 막는 것은 이제 R1이 아니라 **R8-R·R9**다.

---

## 1. 제공 자료 (회신 §5 요구분)

### 1.1 모듈별 top-level `local` — **전부 통과, 최대 46**

200 한계에 걸리는 모듈은 없다. 개별 청크 컴파일에 문제없다.

| 모듈 | top-level local | 그중 `local function` | 바이트 | 줄 |
|---|---:|---:|---:|---:|
| `00_base` | 1 | 0 | 21,970 | 608 |
| `00_navigate` | 1 | 0 | 4,168 | 112 |
| `10_form_wizard` | 4 | 2 | 15,955 | 447 |
| `20_echo` | 0 | 0 | 585 | 15 |
| `30_resolve_zip` | 1 | 0 | 1,053 | 19 |
| `40_read_page` | 1 | 0 | 1,015 | 19 |
| `44_pagination` | 2 | 1 | 5,128 | 117 |
| `45_offer_view` | 29 | 14 | 28,092 | 672 |
| `46_candidate_browser` | 6 | 2 | 6,302 | 160 |
| **`50_commerce`** | **46** | 41 | 77,269 | 1,938 |
| `60_storefront` | 37 | 34 | 33,773 | 824 |
| 사이트 최대 (`amazon/00_common`) | 20 | — | — | — |

수치가 낮은 이유: 함수를 `function M.foo()`로 **테이블에 단다.** top-level `local`은 캐시 별칭과 파일 내부
헬퍼뿐이다.

### 1.2 실제 바이트 — 추정치 정정

```
_common/scripts/*.lua  (공유 모듈 11개)          195,310 B   190.7 KiB
사이트 Lua 합계                                  187,046 B   182.7 KiB
  ├ 전용 어댑터 3개 (amazon 65.2 / thumbtack 76.3 / ebay 17.0)  158.4 KiB
  └ 얇은 CONFIG 9개                                            24.2 KiB
────────────────────────────────────────────────────────────────────
프로덕션 Lua 총계                                            373.4 KiB
_common/flows.yaml (플로우 구조, Lua 제외)                    190.5 KiB
                                                    합계     563.9 KiB

playground Lua 64.2 KiB + playground flows 19.5 KiB       =   83.7 KiB
```

| | 그쪽 추정 | **실측** |
|---|---:|---:|
| 밀도 | 36.8 B/line | **39.6 B/line** |
| 공유 모듈 | ~167 KiB | **190.7 KiB** |
| 전체 자산 | ~356 KiB | **373.4 KiB** |

손익분기 26 B/line 판정은 유효하다 — 근처에도 못 간다.

### 1.3 `egressAllowlist` FQDN — 4개

전체 Lua 스캔 결과 fetch 대상은 4개뿐이다(나머지 21개 URL은 `nav.navigate` 대상인 사이트 주소).

| FQDN | 용도 | 빈도 | 캐시 |
|---|---|---|---|
| `api.frankfurter.dev` | 환율 (base=USD) | 비교 1회당 1 | **`cacheMs` 3,600,000 (1h)** |
| `photon.komoot.io` | 지오코딩 1순위 | 견적 1회당 ≤1 | 없음 (입력이 매번 다름) |
| `nominatim.openstreetmap.org` | 지오코딩 폴백 | 1순위 실패 시 | 없음 |
| `geocoding.geo.census.gov` | ZCTA 역조회 / onelineaddress | 좌표 확보 후 1 | 없음 |

전부 `https`, 인증 없음, 공개 API. `net.maxCalls` **기본 4를 그대로 쓴다** — 폴백 체인 최대 3 + 환율 1에
정확히 맞는다. 지오코딩은 공개 무료 인스턴스라 우리 쪽에서 ZIP 결과를 flow state에 유지해 같은 주소를 두 번
묻지 않겠다.

### 1.4 `dom.query_all` `fields` 계약 정의 (R5 요청분)

우리가 지금 쓰는 형태다. **10개 사이트 리더가 이것에 걸려 있으므로 이대로 `types`에 실어 정본화해 달라.**

```jsonc
{
  "text":  true,                                  // 행 루트의 textContent (공백 정규화)
  "url":   { "selector": "a.item", "attr": "href" },
  "name":  { "selector": ".title" },              // attr 생략 → textContent
  "id":    { "attr": "data-listing-id" },         // selector 생략 → 행 루트 자신
  "price": { "selector": ".price .value" }
}
```

| 규칙 | 의미 |
|---|---|
| `true` | 행 루트의 textContent |
| `{ selector }` | 행 **내부** 첫 매치의 textContent |
| `{ attr }` | 행 루트의 해당 속성 |
| `{ selector, attr }` | 행 내부 첫 매치의 해당 속성 |
| 매치 없음 | **에러가 아니라 키 없음** |
| `limit` | 호출자가 항상 지정 (우리는 24) |

**"매치 없으면 키 없음"이 계약의 핵심이다.** 우리 리더는 필드별 폴백 체인(`data-test` → 시맨틱 → 구조)을
쓰고 **부분 결과를 허용**한다. 한 필드가 없다고 행을 버리면 사이트 개편 때마다 결과가 0이 된다.

### 1.5 문서 인용

최신본(op 어휘 §2 / 와이어 계약 §3 / 시나리오 §6 / 참조 구현 §12)을 받아 이 저장소에 두고 반영했다.
우리 문서의 인용도 최신 번호로 고쳤다.

---

## 2. 신규·개정 요청

### R8-R · 문서 타게팅 — 스티키 탭 바인딩 — **P0, Phase 4 차단**

**개정 사유**: `target: { urlPattern }` 은 **정지 상태**를 가정한다. 우리 스크립트는 target으로
**이동해 들어간다.**

```lua
local from = dom.get_location_href()             -- ① 현재 페이지(google.com일 수 있음)
nav.navigate("https://search.11st.co.kr/...")    -- ② fire-only
nav.wait_for_navigation(from, {...})             -- ③
dom.wait_for_selector(config.result_ready)       -- ④
dom.query_all(...)                               -- ⑤ 여기서야 11번가다
```

툴 레벨에 `urlPattern: "^https://search\.11st\.co\.kr/"` 을 걸면 **①에서 전원이 `not_eligible` → 즉시
`no_client`** 로 죽는다. 스크립트가 이동을 시작하지도 못한다. 같은 형태가 견적(google → thumbtack),
`checkout`(임의 페이지 → 쇼핑몰)에도 적용된다.

**요청**: 대상을 **URL이 아니라 탭에 고정**한다.

> 스크립트의 **첫 op에 답한 문서의 탭**을 그 스크립트 실행 동안 고정하고, 이후 op은 그 탭에만 보낸다.
> 탭 안에서 문서가 교체되어도(=우리의 이동) 바인딩은 유지된다.

```yaml
rpc:
  target:
    bind: script                                          # first-answer 탭에 고정
    urlPattern: "^https://(www|search)\\.11st\\.co\\.kr/"  # 선택: 초기 후보 좁히기
```

**요구 사항**
1. 바인딩은 **스크립트 1회 실행** 범위. 종료 시 해제.
2. 바인딩된 탭이 사라지면 다음 op은 `no_client`(조용한 대기 금지).
3. 서버는 탭을 알 필요 없다 — 클라이언트가 첫 응답에 안정적 탭 식별자를 싣고 런타임이 이후 프레임에
   되돌려 주면 된다. **판정은 클라이언트, 전달은 서버**라는 기존 모델을 지킨다.
4. `urlPattern`은 초기 후보 선택에만 쓴다(이미 여러 탭이 열려 있을 때).

**수용 기준** — 그쪽 제시안에 한 줄 추가:
- 탭 2개(A: 사이트 A, B: 사이트 B)에서 A를 대상으로 한 스크립트는 항상 A에서만 실행된다.
- **그리고 그 스크립트가 A 탭을 사이트 B로 이동시켜도 계속 A 탭에서 실행된다.**

**대안(스티키가 크면)**: `target`의 **op 단위 override**. ①~③은 대상 없음, ④~⑤는 목적지 패턴. 장황하지만
동작한다.

**미제공 시**: `target`을 사용하지 못한다. Phase 4의 다중 탭 안전성이 클라이언트 `isEligible`에만 의존하고,
쓰기 op(장바구니)이 다른 탭에서 실행될 위험을 `rpc.fanout()` 사후 감지로만 다루게 된다.

---

### R9 · 프로덕션 문서 전달 경로 — **P0, Phase 2 이후 차단**

**근거**: R2를 완벽히 구현해 Lua를 **전부** 문서 밖으로 빼도 남는 플로우 구조가 **190.5 KiB = 상한의 74%**다.

| 구성 | 크기 | 비율 |
|---|---:|---:|
| 프롬프트·설명·terminal 문구 | 48.0 KiB | 25% |
| 그래프 + 툴 파라미터 스키마 | 126.3 KiB | 66% |
| 기존 인라인 in-engine Lua | 16.1 KiB | 8% |

74%는 여유가 아니다. 이번 분기에만 스크리닝 노드 3개·페이징 루프·재시도 경로가 늘었다. 프롬프트 중복
정리로 5~8 KiB는 줄이겠지만 **줄여서 될 문제가 아니다.**

**요청 — 아래 셋 중 하나로 경로를 확정해 달라**

| | 방법 | 확인 필요 |
|---|---|---|
| **A** | 본체를 **앱 패키지**에 두고 `clientFlows`는 오버레이 전용 | 앱 패키지 문서에도 256 KiB가 적용되는가? 우리 저장소의 배포 절차는? |
| **B** | **gzip 수신**으로 실효 용량 확대 | YAML 압축률이면 3~4배. R3-3을 먼저 넣는 것으로 해소되는가? |
| **C** | 상한 상향 | 그쪽이 반대한 안. 우리도 선호하지 않는다 |

**A가 사실상 확정이라면** 전달 모델의 이해를 바꿔야 한다 — "클라이언트가 문서를 보낸다"가 프로덕션에는
적용되지 않고 `clientFlows`는 **개발 중 오버레이 전용**이 된다. 그 이해가 맞는지 명시해 달라.

**수용 기준**: 190.5 KiB 문서가 **실제로 통과하는 경로**가 문서화되고, 그 경로로 우리 문서가 배포되는 것을
1회 실증한다.

**미제공 시**: Phase 2까지만 진행 가능(현재 문서 그대로 74%). Phase 3에서 툴별 `rpc`/`allow`/`modules`
블록이 얹히면 76%가 되고, 이후 기능 추가마다 벽에 가까워진다.

---

### R10 · 모듈 청크의 `_ENV` 계약 명세 — **P0(작업량 결정), 소규모**

**요청**: 개별 청크로 컴파일되는 모듈들이 **전역 환경을 공유하는지 격리하는지**를 스펙에 명시하고 테스트로
고정해 달라. 현재 어느 문서에도 없다.

**근거**: 우리 모듈은 전역으로 노출하고 전역으로 참조한다.

| | 값 |
|---|---|
| `return`이 있는 모듈 | **0 / 11** — 전부 `AX_BASE = {...}` 형태 |
| 교차 모듈 전역 참조 | **78건 / 37개 파일** (`AX_BASE` 12, `AX_THUMBTACK` 12, `AX_OFFER_VIEW` 12, `AX_COMMERCE` 12, `AX_STOREFRONT` 11, `AX_PAGINATION` 9, `AX_AMAZON` 9, `AX_WIZARD` 6, `AX_EBAY` 4) |

- **공유한다면** → 우리는 `return M` 한 줄씩만 추가한다(11개 파일).
- **격리한다면** → 78곳을 `local B = modules["00_base"]`로 바꾼다. 기계적이지만 평가 순서 의존이 생겨
  빌더가 순서를 보증해야 한다.

**어느 쪽이든 수용한다. 답이 없으면 착수를 못 한다.**

**수용 기준**: 스펙에 한 문장 + 해당 동작을 고정하는 테스트 1건.

---

### R3-R · gzip 수신 우선순위 상향 — **P1 → P0**

**요청**: R3의 세 항목 중 **gzip 수신(3번)을 먼저** 내 달라.

**근거**: 지금 `content-encoding: gzip`으로 보내면 **HTTP 500**이다(그쪽 실측). 그 자체로 결함이고, 동시에
R9의 B안이다. YAML은 압축률이 높아 190.5 KiB 문서가 실효로 크게 줄어든다. **R9의 구조적 결정(A안)을
논의하는 동안 시간을 벌어 주는 유일한 항목**이다.

**수용 기준**: gzip 본문이 200으로 처리되고, 우리 190.5 KiB 문서가 압축 전송으로 통과한다.

---

### R1-C · `net.fetch` 착수 확인 — **P0 (확인)**

설계는 그쪽 안을 그대로 수용한다. 값 반환(예외 아님) 비대칭에 동의한다 — HTTP 실패는 정상 운영 중
일어나므로 스크립트가 분기할 대상이 맞다.

**요청**: 인프라(런타임 아웃바운드) 승인이 임계 경로라고 했으니 **예상 일정을 회신해 달라.**
Phase 4~5 착수 시점을 그것에 맞춘다. FQDN 4개는 §1.3에 있다.

---

### R2-C · sha256 핸드셰이크(2단계) 근거 — **P1 (확인)**

1단계(개별 청크 + `execute.modules`)가 correctness라는 순서에 동의한다.

**2단계가 우리에게 특히 크다**: 공유 모듈이 **190.7 KiB**다. 세션마다 올리면 하루 1만 세션 기준 1.9 GB다.
내용 주소 방식이 들어오면 두 번째 세션부터 수백 바이트가 된다. **1단계 완료 후 2단계 일정을 알려 달라.**

---

## 3. 확인만 필요한 항목

| ID | 상태 |
|---|---|
| **R4** 관측성 | 수용. `params` 마스킹·길이 절단 동의. 노드 집계(`rpcOps`, `rpcMs`)가 우리 수용 기준 2를 충족 |
| **R5** op 버전 | 수용. `version` 해시를 `check:rpc-ops`로 CI 고정. `fields` 계약은 §1.4 |
| **R6** 폴 예산 | 수용. 우리가 원한 것이 정확히 그것이다 — `maxCalls`를 실제 op 수로 걸겠다 |
| **R7** `page.eval` | **철회.** 환율 조회가 그 순간 열린 쇼핑몰 탭의 CSP에 종속된다는 지적이 맞다. 쓰지 않는다 |
| **Q1** `kind: remote` | 유지된다는 답 확인. R1 도착 후에는 쓰지 않는다 |
| **Q2** 전송 시점 | **매 턴 교체**를 개발 루프로 채택. 우리 `ax sync` + `reload-ext`가 사라진다 |
| **Q3** `flow.map` 대상 | 부모와 같은 브로드캐스트 확인 → R8-R의 근거 |
| **Q4** `contexts` | **몰랐다.** 비교 스냅샷을 `contexts`로 옮기도록 설계 반영(D12) |
| **Q5** 컴파일 폭발 반경 | 문서 전체 무효화 유지에 동의. 그래프 검증은 우리 CI에 둔다 |

---

## 4. 일정·의존성

```
Phase 0 (기반)         ─────────────────────  차단 없음, 착수 가능
Phase 1 (Playground)   ─────────────────────  차단 없음, 착수 가능 (83.7 KiB)
Phase 2 (순수 모듈)    ──── R2 1단계 · R10 ────
Phase 3 (스토어프론트) ──── R2 · R3-R/R9 ─────
Phase 4 (비교)         ──── R1 · R8-R · R9 ───
Phase 5 (견적)         ──── R1 · R8-R ────────
```

**회신이 필요한 시점**

| 항목 | 언제까지 | 없으면 |
|---|---|---|
| **R10** (`_ENV`) | Phase 2 착수 전 | 78곳 정리 방향을 못 정한다 |
| **R8-R** (타게팅) | Phase 4 설계 전 | 다중 탭 안전성을 사후 감지로만 다룬다 |
| **R9** (전달 경로) | Phase 3 완료 전 | 만든 문서를 배포할 곳이 없다 |
| **R1-C** (일정) | 회신 시 | Phase 4~5 일정을 못 세운다 |

---

## 5. 우리가 하는 일

1. **Phase 0~1 착수** — 차단 없음. Playground(83.7 KiB)를 RPC Lua로 옮겨 첫 레퍼런스로 삼는다.
2. R10 답변 즉시 `return M` 추가 + 교차 참조 78곳을 일괄 정리한다.
3. R2 1단계 도착 시 **실제 문서를 만들어 바이트를 재서** 보고한다. 추정으로 넘기지 않는다.
4. 프롬프트 중복(스토어 목록·취소 규칙이 여러 노드에 반복) 5~8 KiB를 줄인다 — R9와 무관하게 할 일이다.
5. `check:rpc-ops`를 R5 `version` 기준으로 CI에 넣는다.
6. 모든 스크립트에 `rpc.allow` 최소 선언, 되돌릴 수 없는 조작은 노드 분리 유지, 쓰기는 `rpc.fanout()` 가드.

---

## 6. 우선순위 요약

| ID | 항목 | 우선 | 성격 | 없으면 |
|---|---|:---:|---|---|
| **R10** | 모듈 `_ENV` 계약 명세 | **P0** | 문서 1문장 + 테스트 | Phase 2 착수 불가 |
| **R3-R** | gzip 수신 | **P0** | 결함 수정 겸 R9 완화 | 500 에러 유지, R9 우회로 없음 |
| **R8-R** | 스티키 탭 바인딩 | **P0** | 설계 변경 | Phase 4 다중 탭 안전성 미확보 |
| **R9** | 문서 전달 경로 | **P0** | 정책 결정 | Phase 3 산출물을 배포할 곳 없음 |
| **R1-C** | `net.fetch` 일정 | **P0** | 회신 | Phase 4~5 일정 미정 |
| **R2-C** | sha256 2단계 일정 | P1 | 회신 | 세션당 190.7 KiB 반복 전송 |
| R4/R5/R6 | 관측성·버전·폴 예산 | P1 | 수용 완료 | — |
