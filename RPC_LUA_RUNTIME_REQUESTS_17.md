# RPC Lua 런타임 요청 17 — 종결: 요청 3건 모두 우리 쪽 결함이었습니다 (정정 + 승인 회신)

날짜: 2026-08-16 · 대상: agentv3 runtime · 작성: axsdk-sites
근거: 라이브 실측. `check:flows` 137 · `test:lua` 525 · memory 라이브 **9/10** · playground 라이브 7/7.
이력: 첫 판(R17-1/R17-2) → 정정판(R17-2 철회, R17-3 신규) → **이 판(전부 종결)**

**신규 요청 없음.** 회신 감사합니다 — 세 건 모두 그쪽 지적이 맞았고, 우리 쪽에서 끝냈습니다.

---

## 1. 결론

| 요청 | 결과 |
|---|---|
**R17-1** 원문 메시지 노출 | **철회.** 경로가 이미 있었습니다. 기능 공백이 아니라 **우리 조사 실패**였습니다. |
**R17-2** 훅이 턴을 멈춤 | **철회.** 훅은 무죄입니다. |
**R17-3** 간헐 무실행 | **철회.** 402와 우리 컴파일 오류의 합이었습니다. |

memory 라이브 스위트 **6/10 → 9/10**. 남은 1건은 이 문서와 무관한 별개 갭입니다(§5).

---

## 2. R17-1 — 저희가 존재하지 않는 이름을 찾고 있었습니다

`userMessages`가 모든 action 노드 상태에 주입되고 `messagePolicy`가 원문/세그먼트를 정한다는 설명대로,
그대로 동작했습니다.

```yaml
handle:
  kind: action_unit
  messagePolicy: { userText: original }
  inputSelector: [ requestText, userMessages ]
```

저희 프로브가 `original=nil`을 보고한 이유는 **`originalUserText`라는 없는 필드를 찾았고
`userMessages`를 선택조차 하지 않았기 때문**입니다. 프로브가 인자 키를 전부 출력했는데도
(`keys=requestText`) 그것이 "선언한 것만 온다"는 뜻임을 저희가 읽어내지 못했습니다 — 요청 16에서
직접 확인받은 규칙인데도 같은 벽에 다시 부딪혔습니다.

새 필드를 만들지 않은 판단(같은 사실의 두 이름, `messagePolicy`를 무시하는 두 번째 경로)에
**동의합니다.** 스펙에 `userMessages` 스키마 형태를 넣어 주신 것도 감사합니다 — 저희가 세 번의
라이브 실행으로 좁힌 내용입니다.

---

## 3. R17-2/R17-3 — 402, 그리고 저희 컴파일 오류

### 402가 보이지 않던 이유 (우리 결함)

저희 드라이버는 `POST /sessions/message`의 **HTTP 상태를 보지 않았습니다.** 그래서 쿼터 거부와
"정상인데 조용한 턴"이 같은 관측이었습니다. 훅이 턴당 flow를 하나 더 돌려 한도에 더 빨리 닿는다는
설명이 "훅 제거 → 즉시 복귀"까지 정확히 설명합니다.

고쳤습니다: 턴이 **노드를 하나도 실행하지 않은** 경우에만 확장의 `axsdk:errors`를 읽어 상태·코드를
함께 보고합니다. 세 가지 제약을 두었습니다 — 멈춘(stalled) 턴은 이미 노드 이름을 말하므로 읽지 않고,
기록이 없으면 문장을 바꾸지 않으며(원인을 만들지 않음), 전체 페이로드는 절대 출력하지 않습니다(요약만).

### 그리고 저희 컴파일 오류가 하나 더 있었습니다

멈춤을 쫓다가 훅의 쓰기에서 `consent: required`를 제거했는데, 플랫폼이 정확히 거부합니다:

```
adapters.tools.write_captured_memory.consent must be required for mutation adapters
```

문서 전체가 컴파일에 실패하고, **밖에서 보면 멈춘 턴과 똑같습니다** — 도구가 하나도 안 돕니다.
즉 정정판에 적은 "consent 제거로도 변화 없음"은 **깨진 문서를 측정한 것**이었습니다. 복원 후
스위트가 1/10 → 7/10으로 올랐습니다.

**이 두 가지가 합쳐져 저희가 훅을 범인으로 지목했습니다.** 훅 3케이스 실측과 로그 개선
(`hook_started`/`hook_finished`)에 감사드립니다 — 특히 시작/종료 구분이 없어 조사가 길어졌다는
지적은 저희 쪽 진단 도구에도 그대로 적용됩니다.

---

## 4. 설계 제안에 대한 회신 — `hooks.timeoutMs`

**옵트인 + 기본 무제한에 동의합니다.** 훅이 무엇을 하는지는 앱이 알고, 임의로 자르지 않는 편이 맞습니다.

저희 `beforeIntent`는 `rpc`(`memory.set_bulk`)를 씁니다. 권고대로 **구현되면 즉시 걸겠습니다.**
현재는 도구 자체의 `deadlineMs: 15000`으로 막고 있고, 훅 비용 = 턴 비용이라는 지적에 따라 훅은
왕복 **1회**(쓰기)로 유지하고 캡처는 순수 계산으로 두었습니다.

요청드릴 값이 있다면 `timeoutMs: 5000` 정도가 저희 훅에는 충분합니다.

---

## 5. 남은 것 (이 문서의 요청 아님)

memory 스위트의 마지막 1건은 **seed된 메모리를 견적 flow가 재사용하지 않는** 문제입니다. 메모리를
직접 심고 연락처 없는 견적을 보내면 `verify_request`가 완료되지 않고 다시 묻습니다. 캡처와 무관한
별개 갭이며, 저희 쪽에서 먼저 조사합니다.

부수적으로 저희 스위트의 케이스 C가 **자기가 방어해야 할 규칙의 반대를 단정하고** 있었습니다 —
"견적 답변으로 준 연락처는 자동 저장된다". 플래너는 반대로 명시합니다("재사용 가능하다는 이유만으로
저장되지 않는다"). 규칙을 검증하는 형태로 다시 썼고(부정 케이스 먼저), 이제 둘 다 통과합니다.

---

## 6. 현재 상태

- `hooks.beforeIntent: [record_memory]` 배선 완료. 캡처는 순수 계산(오프라인 테스트 16개, 변이 4종
  검사), 쓰기는 왕복 1회. 동의 경계는 순수 조건입니다 — **명시적 절이 없으면 캡처하지 않습니다.**
- memory 라이브 **9/10** · playground 라이브 7/7 · `check:flows` 137 · `test:lua` 525 ·
  `test:playground` 80 · `test:scenarios` 77.
- 커밋 `208219a`.
