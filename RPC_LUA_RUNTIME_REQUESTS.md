# 작업지시서 — RPC Lua 이행을 위한 backend/runtime 요청

| | |
|---|---|
| **발신** | `axsdk-sites` (클라이언트 저작 agentic task) |
| **수신** | backend / runtime |
| **근거 문서** | `AGENTIC_TASKS_RPC_LUA.md` · `rpc_lua_authoring.md` · `rpc_lua_implementation.md` · `FLOWS.md` §9.1.1 / §9.2.1 / §9.2.2 |
| **관련 설계** | `RPC_LUA_MIGRATION.md` (이 저장소) |
| **상태** | 검토 요청 |

## 0. 배경 (한 문단)

`axsdk-sites`는 10개 커머스 사이트 + Thumbtack 견적 + BlueMoonSoft를 구동하는 flow와 Lua **약 9,900줄**을
소유한다. 지금은 브라우저에 주입되는 `kind: remote` 명령 **38개**로 동작하며, durable/defer 제거 방침에 따라
전량을 RPC Lua로 이행하려 한다. 자산의 **81%(약 4,000줄)는 이미 순수 계산**이라 로직 변경 없이 런타임으로
옮겨간다. 이행을 막거나 품질을 떨어뜨리는 항목만 아래에 정리했다. 각 항목에는 **왜 필요한지의 측정 근거**와
**수용 기준**을 붙였다.

---

## 1. 요청 항목

### R1 · 아웃바운드 HTTP — **P0, 차단**

**요청**: 런타임 Lua에서 외부 HTTP를 호출할 수단. 둘 중 하나면 된다.
- (a) RPC Lua 샌드박스의 호스트 프리미티브 `net.fetch(url, opts)` — `rpc.now`/`rpc.sleep`과 같은 층, 왕복 없음
- (b) flowTool 어댑터 `implementation: http` — `execute`에 URL/메서드/헤더, 결과를 노드 출력으로

**필요 이유**: RPC op 어휘가 `nav`/`dom`/`page`뿐이고 런타임 Lua 어댑터에도 HTTP가 없다(`FLOWS.md` §9.2 어댑터
목록). 다음 두 기능이 **전적으로** 외부 HTTP에 의존한다.

| 기능 | 호출처 | 무엇이 걸려 있나 |
|---|---|---|
| 환율 정규화 `C.fetch_fx_rates` | `frankfurter.dev/v1/latest` | **다중 스토어 총액 비교의 근간.** 쿠팡 KRW와 아마존 USD를 한 축으로 비교하는 유일한 수단 |
| ZIP 해석 `B.resolve_zip` | Photon/Komoot → Nominatim → Census ZCTA | Thumbtack 견적의 필수 입력 (US-only) |

환율 없이 비교하면 `19,400 KRW`와 `13.95 USD`를 같은 수로 정렬하게 된다. 그것은 기능 저하가 아니라 **틀린 답**
이라 출시할 수 없다.

**요구 사항**
1. 도메인 **allowlist**(앱/문서 단위). 임의 URL 호출은 원하지 않는다.
2. 자격증명 미전송(쿠키·인증 헤더 없음). 공개 API만 쓴다.
3. 타임아웃과 응답 크기 상한. 초과는 **값으로** 관측 가능한 실패(§`FLOWS.md` 실패 표와 동일한 형태).
4. 응답은 문자열(그대로) 또는 파싱된 테이블. 우리는 패턴 매칭으로 읽으므로 문자열이면 충분하다.
5. `maxCalls`/`deadlineMs` 회계에 어떻게 잡히는지 명시.

**수용 기준**
- flowTool 하나에서 `frankfurter.dev/v1/latest?base=USD`를 호출해 `rates.KRW`를 노드 출력으로 얻는다.
- allowlist에 없는 도메인은 스크립트가 잡을 수 있는 오류로 실패한다.
- 네트워크 실패 시 노드가 멈추지 않고 분기 가능한 값/오류가 된다.

**미제공 시**: 이행 Phase 4(다중 스토어 비교)와 Phase 5(견적)를 **시작하지 않는다.** Phase 1~3만 진행하고
비교 기능은 현행 durable 경로에 남는다 — 그 경로는 현재 결함으로 동작하지 않는다(§4 참조).

**대안 확인 요청**: `page.eval`로 페이지에서 `fetch`하는 우회를 과도기 수단으로 허용할지. 허용한다면 R7 참조.

---

### R2 · 문서 레벨 Lua 모듈 재사용 — **P0**

**요청**: 한 flow 문서 안에서 Lua 모듈을 **한 번 선언하고 여러 flowTool이 참조**하는 수단.
- (a) 문서 레벨 `luaModules: { <name>: <source> }` + `execute.lua`에서 `require("<name>")`, 또는
- (b) `execute.modules: [<name>...]` — 런타임이 선언 순서대로 앞에 붙여 컴파일

**필요 이유**: 현재 계약은 flowTool 하나 = Lua 청크 하나다. 우리 공유 모듈은 아래와 같고, **여러 툴이 같은
모듈을 쓴다.**

| 모듈 | 줄 | 참조하는 툴(예상) |
|---|---:|---|
| `00_base` (텍스트/파싱/셀렉터 유틸) | 607 | 거의 전부 |
| `44_pagination` + `45_offer_view` + `50_commerce` | 2,724 | 랭킹·윈도우·리파인·스크리닝 등 **6개 이상** |
| `60_storefront` (+ 사이트 CONFIG 10개) | 823 + 약 500 | 검색·장바구니 |

툴마다 복제하면 같은 2,700줄이 6번 실려 **문서가 수 MB로 부푼다.** 매 세션 전송 비용과 컴파일 비용이
그대로 곱해진다.

§9.2.2의 세션 모듈 주입(`POST /axsdk/v2/lua`)이 이미 같은 엔진을 쓰지만 **flowTool에서 참조할 수 없다**
(REST invoke 전용). 그 경계만 열어 주면 된다.

**수용 기준**
- 2,700줄 모듈을 문서에 1회 선언하고 6개 flowTool이 참조했을 때, 전송 페이로드에 소스가 **1벌만** 존재한다.
- 존재하지 않는 모듈 참조는 **컴파일 시점** 오류(런타임 첫 호출까지 미루지 않는다).
- 모듈 간 의존 순서를 선언 순서로 보장한다(우리 파일들은 `00_ → 44_ → 45_ → 50_` 순서 의존).

**미제공 시**: 툴별 번들링으로 진행은 가능하나(설계 §3.1) 페이로드가 6배 부풀고 R3의 상한에 먼저 부딪힌다.

---

### R3 · `clientFlows` 페이로드 상한 공개 — **P1**

**요청**: 세션 생성 시 `clientFlows` 문서의 **크기 상한**과 초과 시 동작을 문서화.

**필요 이유**: 우리 문서는 Lua 인라인 후 최소 5,000줄, R2 미제공 시 40,000줄 규모가 된다. 지금은 상한이
문서에 없어 "얼마까지 되는지 보내 보고 알아내는" 상태다.

**수용 기준**: 상한 수치가 문서에 있고, 초과 시 **명확한 오류 코드**로 거부된다(잘림·무음 실패 금지).
가능하면 압축 전송(gzip) 지원 여부도 명시.

---

### R4 · RPC 실행 관측성 — **P1**

**요청**: 노드 실행 중 발생한 RPC 호출을 런타임 상태에서 조회 가능하게. 항목당
`op` · `params 요약` · `소요 ms` · 결과 코드 · `fanout {executed, declined, silent}`.

**필요 이유**: 지금 우리는 **확장 측 텔레메트리**(`call received → claim:acquired → execution:start → completed`
+ HTTP status)로 장애를 진단한다. 최근 두 건의 결함(진입 노드 결과 유실, defer 재실행 루프)은 **그 로그가
아니었으면 원인을 특정할 수 없었다.** RPC로 옮기면 실행 주체가 런타임이 되어 그 로그가 사라진다.

`AGENTIC_TASKS_RPC_LUA.md` §10-5는 `configRuntimeState.stepOutputs`와 `__error.message`를 안내하는데,
이는 **결과**만 보여 준다. "어느 op에서 몇 ms 걸려 무엇으로 실패했는가"가 필요하다.

**수용 기준**
- 실패한 노드에 대해 `GET <runtime>/session/<id>/state`에서 그 노드가 실행한 op 목록과 각 결과를 볼 수 있다.
- 성공한 노드도 op 수와 총 왕복 시간을 볼 수 있다(왕복 폭증을 리뷰에서 잡기 위함).

---

### R5 · op 계약의 버전 고정 수단 — **P1**

**요청**: `GET /axsdk/v2/lua/ops` 응답에 **버전 또는 해시**를 포함. 더불어 `dom.query_all`의 `fields` 스키마를
응답에 명시.

**필요 이유**: 저작 가이드가 "문서를 믿지 말고 받아서 쓰라"고 지시한다(`AGENTIC_TASKS_RPC_LUA.md` §10-1).
우리는 이를 CI 게이트(`check:rpc-ops`)로 고정하려 하는데, 지금 응답에는 비교할 식별자가 없어 op 목록 전체를
스냅샷하는 수밖에 없다 — 순서만 바뀌어도 깨진다.

`fields`를 명시해 달라는 이유: 우리 리더가 `{ text = true, url = { selector, attr } }` 형식을 쓴다. 확인 결과
`dom.query_all`이 클라이언트와 같은 `queryLuaElements`를 호출하므로 현재는 호환되지만, **그 호환이 계약인지
우연인지 문서에 없다.** 이 형식이 깨지면 10개 사이트 리더가 동시에 죽는다.

**수용 기준**: 응답에 안정적인 버전 식별자가 있고, `fields` 형식이 응답 또는 문서에 정의되어 있다.

---

### R6 · 폴링 예산 회계 — **P2**

**요청**: 대기 헬퍼(`dom.wait_for_selector`, `nav.wait_for_navigation`)의 폴 호출을 `maxCalls`에서 **제외**
하는 옵션. 또는 폴 전용 예산(`pollBudget`).

**필요 이유**: 문서가 "대기하는 스크립트에는 `maxCalls`를 걸지 마라"라고 지시한다(§8/§9). 그러면 폴이 아닌
실제 op에도 상한을 못 건다. 우리 스토어프론트 스크립트는 **이동 1회 + 대기 2회 + `query_all` 1회**가
기본형이라, 대기를 제외한 상한 5회 같은 것을 걸고 싶다.

**수용 기준**: 대기 폴을 제외한 op 수에 상한을 걸 수 있다.

---

### R7 · `page.eval` 정책 확인 — **P2 (R1 미제공 시 P0으로 승격)**

**요청**: 우리 앱에서 `page.eval`이 활성인지, opt-in 절차가 무엇인지.

**필요 이유**: R1이 지연될 경우 환율 조회의 **유일한 과도기 경로**다. 남용할 생각은 없다 — 호출 지점을
환율 1곳으로 좁히고, 실패 시 비교를 **거부**한다(틀린 환율로 계산한 총액은 없는 것보다 나쁘다).

---

## 2. 회신만 필요한 질문 (코드 작업 아님)

| # | 질문 | 우리 결정에 미치는 영향 |
|---|---|---|
| Q1 | `kind: remote`를 계속 지원하는가? 지원한다면 net 전용 명령을 남겨도 되는가? | R1 대안 B의 성립 여부 |
| Q2 | `clientFlows`는 세션당 1회 전송인가, 매 턴인가? 개발 중 문서를 갈아끼우는 표준 절차는? | 우리 개발 루프(`ax sync` + `reload-ext`)의 대체 설계 |
| Q3 | `flow.map` 하위 플로우에서 RPC를 쓸 때 대상 문서(탭) 결정은 어떻게 되는가? | 10개 사이트를 순회할 때 **어느 탭이 실행하는가**가 결과를 좌우한다 |
| Q4 | `state: session`이 `(session, tool)` 키인데, 여러 툴이 상태를 공유해야 하면 flow state가 유일한 수단인가? | 비교 스냅샷 보관 위치 |
| Q5 | 문서 컴파일 오류가 **문서 전체**를 무효화하는 현 동작(모든 인텐트가 "플로우 설정을 불러오지 못했습니다")을 유지하는가? | 우리 CI에 그래프 검증을 어디까지 둘지 |

---

## 3. 우리가 하는 일 (약속)

| | |
|---|---|
| 레퍼런스 이행 | Playground를 **먼저** 옮겨 공개 예제로 삼는다(`RPC_LUA_MIGRATION.md` Phase 1). 프로덕션 사용자 영향 없음 |
| op 공백 보고 | 이행 중 발견하는 op 공백은 **측정 근거와 함께** 이 문서에 추가한다. 추측으로 요청하지 않는다 |
| CI 고정 | R5 제공 시 `check:rpc-ops`로 op 계약을 CI에 고정한다 |
| 최소 권한 | 모든 스크립트에 `rpc.allow`를 최소로 선언한다. 읽기 노드에 쓰기 op을 넣지 않는다 |
| 승인 게이트 | 되돌릴 수 없는 조작(장바구니·견적 제출)은 **노드 분리**를 유지한다. 스크립트가 사용자 동의를 대신하지 않는다 |
| 쓰기 브로드캐스트 | 쓰기 op은 `rpc.fanout()`으로 가드한다 |

---

## 4. 일정 의존성

```
Phase 0 (기반)        ──────────────  의존 없음
Phase 1 (Playground)  ──────────────  의존 없음
Phase 2 (순수 모듈)   ──────────────  R2 권장 (없어도 진행 가능, 페이로드 부풀음)
Phase 3 (스토어프론트) ─────────────  R2 필요, R5 권장
Phase 4 (비교 플로우)  ───── R1 차단 ─────
Phase 5 (견적)        ───── R1 차단 ─────
```

**현재 프로덕션 상태**(참고): durable 경로의 결함 두 건으로 `checkout`·`bluemoonsoft`·견적 ZIP 단계가
동작하지 않는다. 진입 노드 remote 결과 유실은 이 저장소에서 우회했고(커밋 `b8e5e8b`), defer 재실행 루프는
우회 수단이 없다. **RPC 이행이 그 두 건의 근본 해소책**이므로 R1·R2가 곧 복구 일정이다.

---

## 5. 우선순위 요약

| ID | 항목 | 우선 | 없으면 |
|---|---|---|---|
| **R1** | 아웃바운드 HTTP | **P0** | 총액 비교·견적 이행 불가 |
| **R2** | 문서 레벨 모듈 재사용 | **P0** | 페이로드 6배, R3 상한 충돌 |
| R3 | 페이로드 상한 공개 | P1 | 시행착오로 알아내야 함 |
| R4 | RPC 관측성 | P1 | 장애 원인 특정 불가 |
| R5 | op 계약 버전 | P1 | 조용한 드리프트 |
| R6 | 폴링 예산 분리 | P2 | 실제 op에 상한을 못 건다 |
| R7 | `page.eval` 정책 | P2→P0 | R1 지연 시 과도기 경로 없음 |
