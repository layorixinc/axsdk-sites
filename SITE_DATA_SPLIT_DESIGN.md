# 사이트 데이터 분리 설계 — `62_rpc_sites.lua` → JSON

**목적 하나**: 선택자를 **데이터**로 만들어, 스토어 심사 없이 갱신할 수 있게 한다.

근거는 `CWS_LAUNCH_PLAN.md` P0-1이다. MV3 정책은 원격 **로직**을 금지하지만 §3.2는 원격 **설정 파일**을
명시적으로 허용한다 — *"where all logic for the functionality is contained within the extension package"*.
지금 선택자는 Lua 파일 안에 있어서 데이터가 아니다.

이 문서는 그 전환의 설계다. 구현은 이 문서가 승인된 뒤.

---

## 1. 무엇이 왜 옮겨지는가

### 현재

```
사이트 어댑터(<site>/scripts/00_common.lua)  ← 사람이 쓰는 원본
        │ npm run build:rpc:sites
        ▼
_common/rpc/62_rpc_sites.lua  27.1 KiB · 919줄 · 10 사이트 · 사이트당 71개 키
        │ modules: 로 선언 → 툴마다 인라인 / 앱 패키지로 전송
        ▼
런타임에서 전역 RPC_SITES
```

### 이 변경이 노리는 것

이번 세션에서 고친 라이브 결함 3건이 **전부 선택자**였다:

| 결함 | 증상 |
|---|---|
| 11번가 배송비 셀렉터가 존재하지 않는 클래스를 가리킴 | 모든 11번가 행이 총액 미확인 → 비교에서 접힘 |
| 아마존 제목 셀렉터가 브랜드 헤딩을 집음 | 모든 행이 "Logitech" → 모델 코드 불일치 → 결과 0 |
| 이베이 카드 셀렉터가 옛 마크업 | 조용히 결과 없음 |

**상점이 마크업을 바꾸면 우리 비교가 조용히 깨진다.** 이것이 가장 잦은 고장이고, 릴리스+심사(며칠~몇 주)를
기다릴 수 없는 유일한 부류다.

### 옮기지 않는 것

| 자산 | 이유 |
|---|---|
| 리더(`61_rpc_storefront`), 모듈 전체 | 로직 — 번들 필수 |
| flows 문서 | flowTool마다 `execute.lua`가 있다 — 로직 |
| **사이트 슬러그를 아는 프롬프트** | 프로덕션 플로우의 **21곳**이 슬러그를 문자열로 언급한다(플래너 규칙, "all Korean stores" 정의 등). **데이터만으로 새 상점을 추가할 수는 없다** — 선택자 교체만 가능하다 |

---

## 2. 설계

### 2.1 생성기가 두 산출물을 낸다

`tools/build-rpc-sites.mjs`는 지금 Lua 하나를 만든다. 앞으로 **같은 어댑터 소스에서 둘**을 만든다.

```
<site>/scripts/00_common.lua        ← 여전히 단일 진실
        │
        ├─→ _common/rpc/62_rpc_sites.lua      번들 기본값 (변경 없음)
        └─→ dist/site-data/sites.json         원격 배포용 데이터 (신규)
```

**둘은 같은 생성기의 같은 입력에서 나온다.** 어긋날 수 없고, 어긋나면 게이트가 잡는다.

### 2.2 JSON 형태

```json
{
  "schema": 1,
  "generated_at": "2026-08-06T00:00:00Z",
  "revision": "sha256:ab12…",
  "sites": {
    "coupang": { "site": "coupang", "home_url": "…", "result_selector": "…", … },
    "11st":    { … }
  }
}
```

- `schema` — 로더가 모르는 버전이면 통째로 무시하고 번들을 쓴다.
- `revision` — 생성기 출력의 해시. 로그·디버깅·게이트가 쓴다.
- `sites` — 어댑터 config를 **통째로** 담는다. 지금 생성기가 그러듯 선택하지 않는다:
  *"어댑터가 선언한 키가 리더에서 빠질 수 없다"* — 이 규칙을 깨면 생성기가 없앤 드리프트가 돌아온다.

### 2.3 로더 — `_common/rpc/60_site_data.lua` (신규, 번들)

```lua
-- 우선순위: 원격 데이터 > 번들 기본값. 단 사이트 단위로.
RPC_SITES = SITE_DATA.merge(BUNDLED_SITES, remote)
```

**규칙 넷.** 각각 이 저장소가 이미 비싸게 배운 것에서 나왔다.

1. **사이트 단위 폴백.** 원격 항목 하나가 깨져도 그 사이트만 번들로 되돌린다. 하나가 아홉을 죽이면 안 된다.
2. **부재는 답이다.** 원격이 없으면 번들이 그대로 동작한다. 오프라인 설치에서도 전부 동작해야 한다 —
   그것이 "모든 로직이 패키지 안에" 있다는 증거이기도 하다.
3. **검증은 최소 불변식만.** `site`가 문자열이고 `result_selector`가 있는가. 71개 키를 전부 검사하면
   스키마가 두 벌이 되고 그 둘이 드리프트한다.
4. **알 수 없는 사이트는 무시.** 원격이 번들에 없는 슬러그를 보내도 채택하지 않는다 — 플로우 프롬프트가
   그 슬러그를 모르기 때문에(§1) 반쪽짜리 상점이 생긴다.

### 2.4 누가 원격 데이터를 가져오는가 — **확장(EXT)**

Lua가 `net.fetch`로 직접 가져오지 **않는다**. 이유:

- 툴마다 `net:` 그랜트가 필요해지고, 사이트 데이터를 쓰는 툴은 거의 전부다.
- 캐시·재시도·오프라인이 Lua 런타임의 관심사가 아니다. 런타임 상태는 턴마다 사라진다.

**확장이 가져와 캐시하고, 런타임에 전역으로 주입한다.** 이 저장소가 정의하는 것은 **계약**뿐이다:

| 항목 | 값 |
|---|---|
| 아티팩트 | `sites.json` (§2.2) |
| 주입 | 런타임 전역 `SITE_DATA_REMOTE` (테이블 또는 nil) |
| 갱신 주기 | EXT 결정. 데이터이므로 정책 제약 없음 |
| 실패 | 조용히 nil. 번들이 답한다 |

**이 저장소만으로 끝낼 수 있는 부분과 EXT가 필요한 부분이 여기서 갈린다.**

### 2.5 단계

| 단계 | 내용 | 저장소 | 행동 변화 |
|---|---|---|---|
| 1 | 생성기가 `sites.json`도 낸다 + 두 산출물 동치 게이트 | SITES | **없음** |
| 2 | 로더 모듈 + 오프라인 테스트(폴백·검증·병합) | SITES | **없음** — 원격이 nil이므로 번들 그대로 |
| 3 | 툴들이 `62_rpc_sites` 대신 로더를 선언 | SITES | 없음 |
| 4 | EXT가 fetch·캐시·주입 구현 | EXT | 원격 갱신 활성화 |
| 5 | 배포 파이프라인(어디에 올리고 어떻게 무효화하는가) | BE | — |

**1~3은 이 저장소에서 독립적으로 배포 가능하고, 그 시점까지 사용자 관점 동작은 완전히 동일하다.**
4가 없으면 그냥 지금과 같은 제품이다. 이 저장소의 방식대로, 위험을 나중으로 미루지 않고 앞에서 없앤다.

---

## 3. 게이트 (구현 시 함께)

| 게이트 | 막는 것 |
|---|---|
| `sites.json`과 `62_rpc_sites.lua`가 같은 생성기 출력과 일치 | 커밋된 산출물이 낡는 것. 이번 세션에 **양방향으로 낡아** 11번가 수정이 프로덕션에 도달하지 못했다 |
| 원격 항목 하나가 깨져도 나머지 사이트가 동작 | 하나가 아홉을 죽이는 것 |
| 원격 없이 열 사이트 전부 동작 | 오프라인·차단 환경에서의 무력화, 그리고 "로직은 패키지 안에" 증명 |
| 원격이 번들에 없는 슬러그를 추가하지 못함 | 프롬프트가 모르는 반쪽 상점 |
| 로더가 `net.*`을 grant받지 않음 | Lua가 몰래 네트워크를 여는 것 |

---

## 4. 크기에 미치는 영향 (부수 효과)

playground는 모듈을 **툴마다 인라인**한다. `17_rpc_sites.lua`(27 KiB)가 4개 툴에 들어가 있다.
사이트 데이터가 런타임 주입 전역이 되면 **인라인에서 빠진다** — 대략 **−80 KiB**, 현재 221.3 KiB에서
크게 여유가 생긴다. 프로덕션은 앱 패키지 전송이라 영향이 작다.

---

## 5. 열린 질문

1. **[확인필요]** 선택자 테이블이 §3.2의 "remote configuration file" 범위인가. One Stop Support 사전 문의.
   이 설계 전체가 이 답에 달려 있다.
2. **BE** — `sites.json`을 어디에 올리는가. 앱 패키지에 얹을 것인가, 별도 CDN인가.
3. **EXT** — 갱신 주기와 캐시 무효화. 상점이 마크업을 바꾼 뒤 사용자에게 닿기까지의 목표 시간.
4. 서명/무결성이 필요한가. 데이터라도 선택자는 **에이전트가 무엇을 클릭할지**를 정한다 — 탈취된
   `sites.json`은 임의 요소 클릭을 유도할 수 있다. **[확인필요]** 이건 보안 검토가 필요하다.

> 4번은 가볍게 넘길 항목이 아니다. "데이터라서 안전하다"는 정책상의 분류이지 보안상의 결론이 아니다.

---

## 부록 — ShopAI Assistant 상세 (경쟁 참고)

우리와 기능이 가장 가까운 공개 확장. 스토어 리스팅 실측:

| 항목 | 값 |
|---|---|
| 버전 / 갱신 | 1.3.0 / **2026-03-24** |
| **크기** | **75.41 KiB** |
| 사용자 | **28명**, 평점 5.0 (6건) |
| 카테고리 | Shopping |
| 데이터 공시 | **"수집·사용하지 않음"으로 선언** |
| 사업자 | Non-trader |

기능 설명: *"상품 페이지를 분석하고 AI로 **다른 상점에 걸쳐 같거나 유사한 상품을 검색**한다"* — 우리
멀티스토어 비교와 같은 범주다.

**리스팅 문구가 그대로 참고 자료다.** 어필리에이트 정책이 듣고 싶어 하는 문장을 정확히 썼다:

> - All actions are user-initiated — you decide when to scan and which offers to open.
> - Activates only when you click "Scan"
> - Opens offers in a new tab only when you choose to view them
> - **No automatic redirects or background behavior**
>
> Affiliate Disclosure: Some links shown by ShopAI Assistant may be affiliate links. …
> **Affiliate links are used only after explicit user action.**

읽어낼 점:

1. **정책 대응 문구의 본보기다.** 우리 리스팅(P1-2)과 설치 전 고지(P1-4)에 같은 구조를 쓰면 된다.
   우리 쪽 근거가 오히려 강하다 — 링크가 "픽 1건당 1건"이고 게이트로 강제된다.
2. **75 KiB.** 인터프리터도 번들 Lua도 없는 크기다. 우리가 P0-1을 번들로 해결하면 이보다 훨씬 커지고,
   그것이 심사 시간에 반영된다(`CWS_LAUNCH_PLAN.md` P0-5).
3. **사용자 28명.** 게시됐다는 것이 성공을 뜻하지 않는다. 같은 범주에 Product Search(4.9),
   Goodscouter, AliSmart, Cheaperly, Amazon Europe Price Compare 등이 줄지어 있고 대부분 작다.
   **차별점은 "AI로 비교한다"가 아니라 그 다음에 있어야 한다.**
