# RPC Lua 런타임 요청 16 — 요청 15는 우리 쪽 결함이었습니다 (정정 + 종결)

날짜: 2026-07-27 · 대상: agentv3 runtime · 작성: axsdk-sites
근거: 앱 `browser-extension` revision 124, `fromRemote: 0`. 라이브 실측.

**요청 없음.** 이 문서는 정정입니다.

---

## 1. 결론

요청 15에서 "`action_contract`의 `tool.args.<스칼라>`가 nil로 도착한다"고 보고했습니다.
**런타임 결함이 아니었습니다.** 원인은 우리 플로우에 있었고, 회신으로 주신 세 답변은 모두
정확했습니다.

- `actionArgs`는 노드 실행 시점의 플로우 상태를 읽는다 — **맞습니다.**
- `mapAdapterInput`은 remote 전용이다 — **맞습니다.**
- 런타임 도구는 `parameters.properties`로 투영된다 — **맞습니다.** 이것이 원인이었습니다.

## 2. 실제 원인

문제의 두 스칼라(`query`, `tried_queries`)가 도구의 `parameters.properties`에 **선언되어 있지
않았습니다.** 선언되지 않은 상태는 투영에서 제외되므로, 상태에는 값이 있고 도구에는 nil이
도착합니다 — 우리가 관측한 그대로입니다. `localState` 스냅샷이 값을 보여준 것도 모순이 아니라
정확히 맞는 동작이었습니다: **노드의 상태 스냅샷과 그 도구의 인자는 서로 다른 두 표면**입니다.

우리 쪽에는 `input:` 블록이 22개 남아 있었습니다. remote 시절의 잔재이며, 런타임 도구에서는
컴파일 오류입니다. 전부 제거하고 누락된 선언을 `parameters.properties`에 추가한 뒤 재시도 루프가
전진합니다.

부수적으로 배운 규칙(문서 §7.6 근처에 한 줄 있으면 도움이 될 부분):

- 누산기는 **nullable이면서 non-required**여야 합니다. `null` 대 `{type: string}`은 거부되고,
  `null` 대 nullable은 통과하며, 부재도 통과합니다.
- 빈 Lua 테이블은 JSON **객체**로 인코딩되어 `{type: array}` 검증을 통과하지 못합니다. 우리는 이
  한 가지로 네 곳에서 서로 다른 증상을 겪었습니다 — 마지막은 팬아웃의 `task.resultSchema`가
  `candidates: Invalid input`으로 한 가게를 통째로 "기술적 실패"로 기록한 건이었습니다.
  검증하는 **모든** 경계에서 빈 목록은 부재여야 합니다.

## 3. 현재 상태

요청 15는 **철회**합니다. 런타임 변경 요청이 아니었습니다.

- 프로덕션 플로우의 `kind: remote` = **0**
- 멀티스토어 discovery 라이브 게이트 **14/14**, 열 개 어댑터 스윕 **35/35**
- `check:flows` 108 · `test:lua` 458

## 4. 남은 요청

없습니다. 요청 15로 열린 항목은 이 문서로 종결됩니다.

---

## 부록 — 우리 쪽에 세운 게이트

같은 실수를 다시 만들지 않기 위해 `check:flows`에 추가한 검사입니다. 런타임 팀에 필요한 것은
아니지만, 다른 플로우 작성자가 같은 함정에 빠지는지 판단하는 데 참고가 될 수 있습니다.

- `action_contract`가 `inputSelector`로 고르는 모든 필드는 그 도구의 `parameters.properties`에
  선언되어 있어야 한다. (선언되지 않은 상태는 조용히 버려지고, 셀렉터와 스키마 양쪽 다 멀쩡해
  보입니다.)
- 스크립트가 반환하는 키는 도구의 `output`에 발행되어야 한다. (계산하고 발행하지 않은 값은
  계산하지 않은 값입니다.)
- 도구가 부르는 모듈과 그 모듈이 스스로 요구하는 모듈이 `modules:`에 모두 있어야 한다.
- 도구의 진입점에서 호출 그래프로 도달 가능한 fetch 호스트는 `net.allow`에 있어야 한다.
